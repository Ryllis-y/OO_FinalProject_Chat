# 敏感词过滤功能说明

## ✅ 功能概述

实现了基于 **AC自动机算法** 的敏感词过滤系统，能够高效地检测和过滤聊天消息中的敏感词。

### 核心特性

- ✅ **AC自动机算法**: O(n+m) 时间复杂度，高效匹配多个敏感词
- ✅ **自动替换**: 敏感词自动替换为 `*` 号
- ✅ **实时过滤**: 消息发送时自动过滤，用户无感知
- ✅ **日志记录**: 记录过滤操作，便于监控

---

## 🔧 技术实现

### 1. AC自动机算法

**AC自动机（Aho-Corasick）** 是一种多模式字符串匹配算法，特点：
- 一次扫描文本，同时匹配多个模式串
- 时间复杂度：O(n+m)，n为文本长度，m为所有模式串总长度
- 空间复杂度：O(Σ)，Σ为字符集大小

**核心组件**：
- **Trie树**: 构建敏感词字典树
- **失败指针**: 实现快速跳转，避免重复匹配
- **BFS构建**: 使用广度优先搜索构建失败指针

### 2. 实现位置

- **核心类**: `com.example.chat.util.SensitiveWordFilter`
- **集成点**: `MessageServiceImpl.processAndSaveMsg()` 方法
- **初始化**: 使用 `@PostConstruct` 在服务启动时自动初始化

---

## 📝 敏感词库（20个）

当前包含的敏感词：

1. 傻逼
2. sb
3. 弱智
4. 白痴
5. 脑残
6. 废物
7. 垃圾
8. 蠢货
9. 混蛋
10. 狗屎
11. 王八蛋
12. 猪头
13. 笨蛋
14. 蠢材
15. 饭桶
16. 窝囊废
17. 死鬼
18. 臭不要脸
19. 变态
20. 神经病

---

## 🧪 测试用例

### 测试 1: 单个敏感词

**发送消息**:
```json
{
  "action": "SEND_PRIVATE",
  "params": {
    "targetUser": "bob",
    "content": "你真是个傻逼"
  }
}
```

**预期结果**:
- 消息内容被过滤为：`"你真是个**"`
- 服务器控制台输出过滤日志

### 测试 2: 多个敏感词

**发送消息**:
```json
{
  "action": "SEND_PRIVATE",
  "params": {
    "targetUser": "bob",
    "content": "你是个弱智，真是个废物"
  }
}
```

**预期结果**:
- 消息内容被过滤为：`"你是个**，真是个**"`

### 测试 3: 英文敏感词

**发送消息**:
```json
{
  "action": "SEND_PRIVATE",
  "params": {
    "targetUser": "bob",
    "content": "你真是个sb"
  }
}
```

**预期结果**:
- 消息内容被过滤为：`"你真是个**"`

### 测试 4: 敏感词在句子中间

**发送消息**:
```json
{
  "action": "SEND_PRIVATE",
  "params": {
    "targetUser": "bob",
    "content": "你好，你真是个笨蛋，再见"
  }
}
```

**预期结果**:
- 消息内容被过滤为：`"你好，你真是个**，再见"`

### 测试 5: 不包含敏感词

**发送消息**:
```json
{
  "action": "SEND_PRIVATE",
  "params": {
    "targetUser": "bob",
    "content": "你好，今天天气真好"
  }
}
```

**预期结果**:
- 消息内容保持不变：`"你好，今天天气真好"`

### 测试 6: 群聊消息过滤

**发送消息**:
```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "群组ID",
    "content": "大家注意，不要做脑残的事情"
  }
}
```

**预期结果**:
- 消息内容被过滤为：`"大家注意，不要做**的事情"`
- 所有群成员收到的都是过滤后的消息

---

## 📊 过滤效果示例

| 原始消息 | 过滤后消息 |
|---------|-----------|
| "你真是个傻逼" | "你真是个**" |
| "sb，弱智" | "**，**" |
| "不要做脑残的事情" | "不要做**的事情" |
| "你好，今天天气真好" | "你好，今天天气真好"（不变） |
| "你是个废物垃圾" | "你是个****" |

---

## 🔍 工作原理

### 1. 初始化阶段（服务启动时）

```
1. 加载敏感词列表（20个）
2. 构建Trie树
3. 构建失败指针（AC自动机核心）
4. 完成初始化
```

### 2. 消息处理阶段

```
用户发送消息
    ↓
MessageService.processAndSaveMsg()
    ↓
SensitiveWordFilter.filter()
    ↓
AC自动机匹配敏感词
    ↓
替换敏感词为 *
    ↓
保存过滤后的消息
    ↓
发送给接收者
```

### 3. AC自动机匹配流程

```
文本: "你真是个傻逼"
      ↓
从根节点开始
      ↓
匹配 "你" → 移动到子节点
匹配 "真" → 移动到子节点
匹配 "是" → 移动到子节点
匹配 "个" → 移动到子节点
匹配 "傻" → 移动到子节点
匹配 "逼" → 匹配到敏感词 "傻逼"！
      ↓
标记位置 [5,6] 需要替换
      ↓
替换为 "你真是个**"
```

---

## 📝 代码结构

### SensitiveWordFilter.java

```java
@Component
public class SensitiveWordFilter {
    // Trie树节点
    private static class TrieNode { ... }
    
    // 初始化AC自动机
    @PostConstruct
    public void init() { ... }
    
    // 构建Trie树
    private void buildTrie() { ... }
    
    // 构建失败指针
    private void buildFailPointer() { ... }
    
    // 过滤敏感词
    public String filter(String text) { ... }
    
    // 检查是否包含敏感词
    public boolean containsSensitiveWord(String text) { ... }
}
```

### MessageServiceImpl.java

```java
@Service
public class MessageServiceImpl implements MessageService {
    @Autowired
    private SensitiveWordFilter sensitiveWordFilter;
    
    @Override
    public Message processAndSaveMsg(...) {
        // 敏感词过滤
        String filteredContent = sensitiveWordFilter.filter(content);
        // 使用过滤后的内容创建消息
        ...
    }
}
```

---

## 🎯 使用场景

### 自动过滤

所有通过 `MessageService.processAndSaveMsg()` 处理的消息都会自动过滤：
- ✅ 私聊消息
- ✅ 群聊消息
- ✅ 所有消息类型

### 日志记录

当消息被过滤时，服务器控制台会输出：
```
用户 alice 发送的消息包含敏感词，已过滤
原始内容: 你真是个傻逼
过滤后: 你真是个**
```

---

## 🔧 扩展功能

### 添加新敏感词

修改 `SensitiveWordFilter.java` 中的 `SENSITIVE_WORDS` 列表：

```java
private static final List<String> SENSITIVE_WORDS = Arrays.asList(
    "傻逼", "sb", "弱智", 
    // 添加新敏感词
    "新敏感词1", "新敏感词2"
);
```

**注意**: 修改后需要重启服务器才能生效。

### 自定义替换字符

修改 `filter()` 方法中的替换字符：

```java
// 当前：替换为 *
result.append('*');

// 可以改为其他字符，如：
result.append('●');  // 实心圆
result.append('█');  // 实心方块
```

---

## ✅ 测试检查清单

- [x] 单个敏感词过滤
- [x] 多个敏感词过滤
- [x] 英文敏感词过滤
- [x] 敏感词在句子中间
- [x] 不包含敏感词的消息正常
- [x] 私聊消息过滤
- [x] 群聊消息过滤
- [x] 过滤日志记录

---

## 🚀 快速测试

### 测试步骤

1. **启动服务器**
   ```bash
   ./mvnw spring-boot:run
   ```

2. **登录用户**
   ```json
   {"action": "LOGIN", "params": {"username": "alice", "password": "123456"}}
   ```

3. **发送包含敏感词的消息**
   ```json
   {
     "action": "SEND_PRIVATE",
     "params": {
       "targetUser": "bob",
       "content": "你真是个傻逼"
     }
   }
   ```

4. **验证结果**
   - 接收者收到的消息应该是：`"你真是个**"`
   - 服务器控制台应该有过滤日志

---

功能已完整实现！🎉

