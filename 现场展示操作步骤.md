# 现场展示操作步骤

## 📊 展示层次与逻辑

### 展示逻辑设计原则

1. **从简单到复杂**：先演示基础功能，再演示高级功能
2. **从单用户到多用户**：先单用户操作，再演示多用户交互
3. **从私聊到群聊**：先演示点对点通信，再演示群组通信
4. **功能连贯性**：每个功能模块内部保持逻辑连贯
5. **突出技术亮点**：重点展示技术实现（敏感词过滤、权限管理等）

### 展示层次结构

```
第一层：用户认证（基础）
    ↓
第二层：好友管理（社交基础）
    ↓
第三层：私聊功能（点对点通信）
    ↓
第四层：群聊功能（群组通信）
    ↓
第五层：高级功能（消息交互、权限管理）
    ↓
第六层：技术亮点（敏感词过滤、多端顶号）
```

---

## 🎯 详细操作步骤

### 前置准备（演示前）

1. **启动服务器**
   ```bash
   ./mvnw spring-boot:run
   ```
   - 确保看到 "Started ChatApplication" 提示

2. **准备浏览器窗口**
   - 窗口1：用户 alice（Chrome）
   - 窗口2：用户 bob（Chrome 无痕/Edge）
   - 窗口3：用户 admin（Firefox/另一个浏览器）

3. **准备测试数据**
   - 账号：alice / 123456
   - 账号：bob / 123456
   - 账号：admin / admin123
   - 敏感词测试文本："你真是个XXX"（XXX为敏感词）

---

## 第一部分：用户认证与在线状态（2分钟）

### 1.1 新用户自动注册

**操作：窗口1（alice）**

1. 打开浏览器，访问 `http://localhost:8080`
2. 输入用户名：`alice`，密码：`123456`
3. 点击"登录"
4. **讲解**："这是新用户，系统会自动注册，无需单独的注册流程"
5. 观察登录成功，进入主界面

**展示点：**
- ✅ 自动注册功能
- ✅ 密码加密存储（BCrypt）

### 1.2 在线状态实时显示

**操作：窗口2（bob）**

1. 打开新窗口，访问 `http://localhost:8080`
2. 输入用户名：`bob`，密码：`123456`
3. 点击"登录"
4. 登录成功后，切换到"在线用户"标签

**操作：窗口1（alice）**

1. 切换到"在线用户"标签
2. **讲解**："可以看到 bob 也在线，这是实时更新的"
3. **展示点：**
   - ✅ 在线状态实时同步
   - ✅ WebSocket 实时推送

### 1.3 多端顶号功能

**操作：窗口3（alice 第二设备）**

1. 打开第三个浏览器窗口（或使用不同浏览器）
2. 使用相同账号 `alice` 和密码登录
3. **讲解**："当同一账号在新设备登录时..."
4. 观察窗口1（alice）显示"连接断开"或被踢下线

**展示点：**
- ✅ 多端顶号功能
- ✅ 旧连接自动关闭

---

## 第二部分：好友管理（2分钟）

### 2.1 发送好友申请

**操作：窗口1（alice）**

1. 切换到"在线用户"标签
2. 找到用户 `bob`
3. 点击 bob 右侧的 "+" 按钮
4. **讲解**："alice 向 bob 发送好友申请"
5. 观察提示消息："好友申请已发送"

**展示点：**
- ✅ 好友申请功能
- ✅ UI 交互（加号按钮）

### 2.2 接受好友申请

**操作：窗口2（bob）**

1. 查看右上角的好友申请图标（应该显示红色提醒点）
2. 点击好友申请图标
3. 查看好友申请列表，看到 alice 的申请
4. 点击"接受"按钮
5. **讲解**："bob 接受了 alice 的好友申请"

**操作：窗口1（alice）**

1. 观察"联系人"标签，bob 应该出现在联系人列表中
2. **讲解**："双方的好友列表都自动更新了，这是实时的"

**展示点：**
- ✅ 好友申请提醒（红色圆点）
- ✅ 接受好友申请
- ✅ 双方好友列表同步更新

### 2.3 删除好友

**操作：窗口1（alice）**

1. 点击联系人列表中的 `bob`，进入聊天界面
2. 点击聊天界面右上角的"删除好友"按钮
3. 确认删除
4. **讲解**："alice 删除了 bob 的好友关系"

**观察两个窗口：**

- 窗口1（alice）：bob 从联系人列表中消失，聊天界面回到初始状态
- 窗口2（bob）：如果正在查看 alice 的信息，也会同步更新

**展示点：**
- ✅ 删除好友功能
- ✅ 双方好友列表同步删除
- ✅ UI 状态同步更新

---

## 第三部分：私聊功能（3分钟）

### 3.1 重新添加好友并发送私聊消息

**操作：快速重新添加好友**

1. 窗口1（alice）：在"在线用户"中点击 bob 的 "+" 按钮
2. 窗口2（bob）：接受好友申请

### 3.2 发送和接收私聊消息

**操作：窗口1（alice）**

1. 在"联系人"标签中点击 `bob`
2. 在输入框输入："你好，这是第一条测试消息"
3. 按 Enter 或点击"发送"
4. **讲解**："消息立即发送，这是 WebSocket 实时通信的效果"

**操作：窗口2（bob）**

1. 观察消息是否实时收到
2. **讲解**："消息是实时推送的，不需要刷新页面"
3. 回复一条消息："收到，我也测试一下"

**观察两个窗口：**

- 消息在双方窗口中实时显示
- 自己发送的消息显示在右侧（蓝色背景）
- 对方发送的消息显示在左侧（白色背景）

**展示点：**
- ✅ 实时消息通信
- ✅ 点对点消息路由
- ✅ UI 实时更新

### 3.3 消息撤回功能

**操作：窗口1（alice）**

1. 发送一条新消息："这条消息会被撤回"
2. 鼠标悬停在这条消息上
3. 点击出现的"撤回"按钮
4. **讲解**："消息在 2 分钟内可以撤回"

**观察两个窗口：**

- 消息从两个窗口中同时消失
- 显示系统提示："消息已撤回"

**展示点：**
- ✅ 消息撤回功能
- ✅ 多端同步撤回
- ✅ 时间限制（2分钟）

### 3.4 消息反应功能

**操作：窗口2（bob）**

1. 鼠标悬停在 alice 发送的任意一条消息上
2. 点击"反应"按钮
3. 选择反应类型（比如 👍）
4. **讲解**："可以对消息进行反应，支持多种反应类型"

**观察：**

- 消息下方显示反应图标和计数
- 如果 alice 也对这个消息反应，计数会增加

**展示点：**
- ✅ 消息反应功能
- ✅ 实时更新反应计数

### 3.5 消息历史查询

**操作：窗口1（alice）**

1. 刷新页面（或关闭重新打开）
2. 重新登录 alice
3. 点击联系人 bob
4. **讲解**："可以看到历史消息自动加载了"

**展示点：**
- ✅ 历史消息存储
- ✅ 登录后自动加载历史消息

---

## 第四部分：群聊功能（4分钟）

### 4.1 创建群组

**操作：窗口1（alice）**

1. 切换到"群组"标签
2. 点击"+ 创建群组"按钮
3. 输入群组名称："测试群组"
4. 在成员列表中选择 `bob`（如果有多个用户，可以选择多个）
5. 点击"创建"
6. **讲解**："alice 创建了一个群组，并添加了 bob 作为初始成员"

**观察：**

- 群组创建成功
- 群组出现在群组列表中
- alice 自动成为群主（可以看到群主标识）

**展示点：**
- ✅ 创建群组功能
- ✅ 群主权限设置

### 4.2 加入群组

**操作：窗口3（admin，或其他用户）**

1. 登录 admin 账号（如果还没登录）
2. 切换到"群组"标签
3. 查看是否有"测试群组"
4. 或者通过群组ID加入（如果有加入功能）

**操作：窗口2（bob）**

1. 切换到"群组"标签
2. 可以看到"测试群组"（因为是初始成员）
3. 点击进入群组

**展示点：**
- ✅ 群组成员管理
- ✅ 被动加入群组

### 4.3 发送群聊消息

**操作：窗口1（alice）**

1. 进入"测试群组"
2. 输入消息："大家好，这是群聊测试"
3. 发送消息

**操作：窗口2（bob）**

1. 进入"测试群组"（如果还没进入）
2. 观察是否收到消息
3. **讲解**："群聊消息会广播给所有群成员"

**展示点：**
- ✅ 群聊消息广播
- ✅ 多用户实时接收

### 4.4 @提醒功能（重点）

**操作：窗口1（alice）**

1. 在群聊输入框中输入：`@bob 请查看这条消息`
2. **讲解**："输入 @ 符号，系统会自动识别用户名"
3. 发送消息

**观察两个窗口：**

- 窗口1（alice）：消息正常显示，@bob 被高亮
- 窗口2（bob）：
  - 在左侧群组列表中，"测试群组"旁边显示红色提醒点
  - 进入群组后，@bob 的文本被高亮显示
  - **讲解**："被 @ 的用户会收到特殊提醒"

**操作：窗口1（alice）- 群主 @ 所有人**

1. 如果 alice 是群主，输入：`@所有人 这是群主通知`
2. 发送消息
3. **讲解**："群主可以 @ 所有人，普通成员只能 @ 特定用户"

**展示点：**
- ✅ @提醒功能
- ✅ 前端解析 @ 符号
- ✅ 后端存储 @ 用户列表
- ✅ 被 @ 用户收到提醒
- ✅ 群主 @ 所有人权限

### 4.5 查看群成员

**操作：窗口1（alice）**

1. 点击聊天界面右上角的"查看成员"按钮（或相应按钮）
2. **讲解**："可以查看群组所有成员，以及他们的角色"
3. 观察成员列表：群主、管理员、普通成员

**展示点：**
- ✅ 群成员列表查询
- ✅ 角色显示（群主/管理员）

---

## 第五部分：权限管理（3分钟）

### 5.1 设置群管理员

**操作：窗口1（alice，群主）**

1. 在群成员列表中，选择 `bob`
2. 点击"设为管理员"按钮
3. **讲解**："群主可以设置其他成员为管理员"
4. 观察 bob 的标识更新为"管理员"

**展示点：**
- ✅ 设置管理员权限
- ✅ 角色权限管理

### 5.2 禁言功能（重点）

**操作：窗口1（alice，群主）**

1. 选择群成员 `bob`（如果是管理员，也可以选择普通成员）
2. 点击"禁言"按钮
3. 设置禁言时长：5 分钟
4. **讲解**："群主或管理员可以禁言成员，设置时长"

**操作：窗口2（bob，被禁言）**

1. 尝试在群聊中输入消息
2. **讲解**："被禁言的用户无法发送消息"
3. 观察输入框被禁用，显示："您已被禁言！剩余 X 分 X 秒解除"
4. 同时群组中会显示系统消息："bob 被管理员禁言 5 分钟"

**展示点：**
- ✅ 禁言功能
- ✅ 前端输入框禁用
- ✅ 倒计时显示
- ✅ 系统消息提示
- ✅ 权限校验（只有群主/管理员可以禁言）

### 5.3 踢出成员

**操作：窗口1（alice，群主）**

1. 选择群成员 `bob`（或另一个成员）
2. 点击"踢出"按钮
3. 确认踢出
4. **讲解**："群主或管理员可以踢出成员"

**观察：**

- bob 被移出群组
- 群组中显示系统消息："bob 被管理员移出群组"
- bob 的窗口中，群组从列表中消失

**展示点：**
- ✅ 踢出成员功能
- ✅ 系统消息广播
- ✅ 权限校验

### 5.4 解散群组

**操作：窗口1（alice，群主）**

1. 点击群组设置（如果有）或群组信息
2. 点击"解散群组"按钮
3. 确认解散
4. **讲解**："只有群主可以解散群组"

**观察：**

- 群组被删除
- 所有成员（包括 bob）的群组列表中，该群组消失

**展示点：**
- ✅ 解散群组功能
- ✅ 群主专属权限

---

## 第六部分：敏感词过滤（重点技术）（2分钟）

### 6.1 敏感词过滤演示

**操作：窗口1（alice）**

1. 在私聊或群聊中输入包含敏感词的文本
   - 例如："你真是个XXX"（XXX 替换为实际敏感词，如"傻逼"）
2. **讲解**："我输入了一条包含敏感词的消息"
3. 发送消息

**观察：**

- 消息中的敏感词被自动替换为 `*`（星号）
- 例如："你真是个***"
- **讲解**："敏感词被自动过滤，这是 AC 自动机算法实现的"

**展示点：**
- ✅ 敏感词过滤功能
- ✅ AC 自动机算法（可以简单讲解算法原理）
- ✅ 实时过滤（发送时过滤）

### 6.2 算法原理讲解（可选，如果时间允许）

**（打开 IDE，展示 SensitiveWordFilter.java）**

1. **讲解**："AC 自动机是一种多模式匹配算法"
2. **讲解**："时间复杂度是 O(n+m)，只需要一次遍历文本"
3. **讲解**："特别适合敏感词过滤这种场景"

---

## 第七部分：系统管理员功能（1分钟）

### 7.1 管理员权限展示

**操作：窗口3（admin）**

1. 登录 admin 账号
2. **讲解**："admin 账号自动拥有系统管理员权限"
3. 观察 admin 的用户名旁边有 👑 标识
4. 在在线用户列表中，可以看到所有用户
5. **讲解**："管理员可以踢出在线用户、全局禁言等"

**展示点：**
- ✅ 系统管理员权限
- ✅ 管理员标识
- ✅ 全局权限管理

---

## 📋 展示时间分配建议

| 部分 | 时间 | 重点 |
|------|------|------|
| 第一部分：用户认证 | 2分钟 | 多端顶号 |
| 第二部分：好友管理 | 2分钟 | 好友申请、删除好友 |
| 第三部分：私聊功能 | 3分钟 | 消息撤回、消息反应 |
| 第四部分：群聊功能 | 4分钟 | @提醒功能（重点） |
| 第五部分：权限管理 | 3分钟 | 禁言功能（重点） |
| 第六部分：敏感词过滤 | 2分钟 | AC 自动机算法 |
| 第七部分：管理员功能 | 1分钟 | 系统管理员 |
| **总计** | **17分钟** | |

**注意：如果时间紧张（12-15分钟），可以：**
- 压缩好友管理部分（快速演示）
- 省略部分消息反应演示
- 简化权限管理演示（只演示禁言）
- 保留敏感词过滤（技术亮点）

---

## 🎯 展示要点总结

### 必须展示的核心功能

1. ✅ **实时通信**：WebSocket 实时推送
2. ✅ **好友管理**：添加、删除好友
3. ✅ **私聊功能**：消息发送、撤回
4. ✅ **群聊功能**：创建群组、@提醒
5. ✅ **权限管理**：禁言、踢人
6. ✅ **敏感词过滤**：AC 自动机算法

### 技术亮点展示

1. **HandlerRegistry 模式**（架构设计）
2. **AC 自动机算法**（算法实现）
3. **多端顶号**（状态管理）
4. **权限体系**（RBAC 设计）

### 展示技巧

1. **边演示边讲解**：每个操作都要说明功能和技术点
2. **多窗口对比**：展示实时同步效果
3. **突出亮点**：重点讲解敏感词过滤和权限管理
4. **错误处理**：如果出错，说明这是学习项目，重点在技术实现

---

## ⚠️ 注意事项

1. **提前测试**：演示前一天测试所有功能，确保无 bug
2. **准备备用方案**：如果某个功能出问题，知道如何快速跳过
3. **时间控制**：注意控制总时间，不要超时
4. **重点突出**：技术亮点（AC 自动机、HandlerRegistry）要重点讲解
5. **保持流畅**：操作要流畅，不要卡顿

---

**祝你答辩顺利！** 🎉

