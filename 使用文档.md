# 聊天系统使用文档

## 📖 目录

- [项目简介](#项目简介)
- [功能特性](#功能特性)
- [环境要求](#环境要求)
- [快速开始](#快速开始)
- [用户指南](#用户指南)
- [技术架构](#技术架构)
- [API 文档](#api-文档)
- [常见问题](#常见问题)

---

## 项目简介

这是一个基于 **Spring Boot + WebSocket** 的实时聊天系统，支持单聊、群聊、消息撤回、@提醒、敏感词过滤等完整功能。系统采用内存存储，无需配置数据库，开箱即用。

### 主要特点

- ✅ **实时通信**：基于 WebSocket 协议，消息即时送达
- ✅ **多端支持**：支持多设备同时登录（同一账号后登录会踢出前一个）
- ✅ **完整功能**：私聊、群聊、消息撤回、@提醒、消息反应等
- ✅ **权限管理**：支持管理员权限，群主/管理员权限体系
- ✅ **安全过滤**：内置敏感词过滤系统（AC 自动机算法）
- ✅ **现代化 UI**：简洁美观的前端界面，类似微信风格

---

## 功能特性

### 1. 用户认证与权限

- **自动注册**：新用户首次登录自动注册，无需单独注册流程
- **密码验证**：已存在用户需验证密码（BCrypt 加密存储）
- **多端顶号**：同一账号在新设备登录时，旧连接会被强制下线
- **管理员权限**：用户名为 `admin` 的账号自动拥有管理员权限
- **在线状态**：实时显示在线用户列表

### 2. 私聊功能

- **点对点消息**：支持用户之间一对一聊天
- **消息历史**：自动保存并支持查看历史消息
- **已读回执**：显示消息已读状态
- **消息反应**：支持对消息进行点赞、点踩等反应

### 3. 群聊功能

- **创建群组**：用户可以创建群组并添加初始成员
- **加入/退出群组**：支持主动加入和被动加入群组
- **群成员管理**：
  - **群主**：可以禁言成员、踢出成员、设置/取消管理员、解散群组
  - **管理员**：可以禁言成员、踢出成员（不能操作群主和其他管理员）
- **@提醒**：
  - 支持 @ 特定成员
  - 群主可以 @ 所有人
  - 被 @ 的用户在左侧群组列表会显示红色提醒
- **消息高亮**：@ 的文本在消息中会高亮显示

### 4. 消息交互

- **消息撤回**：可以撤回 2 分钟内发送的消息，所有端同步更新
- **已读回执**：私聊消息支持已读状态显示
- **消息反应**：支持点赞、点踩、爱心、大笑、难过、生气等反应
- **消息时间**：显示完整的发送时间（年-月-日 时:分:秒）

### 5. 敏感词过滤

- **自动过滤**：消息发送时自动检测并过滤敏感词
- **替换显示**：敏感词会被替换为 `*`
- **高效算法**：基于 AC 自动机算法，支持多模式匹配

### 6. 管理功能

- **系统管理员**：
  - 踢出在线用户
  - 全局禁言用户
- **群主权限**：
  - 禁言群成员（指定时长）
  - 踢出群成员
  - 设置/取消群管理员
  - 解散群组
- **群管理员权限**：
  - 禁言群成员（指定时长）
  - 踢出群成员（不能操作群主和其他管理员）

---

## 环境要求

### 必需环境

- **Java**: JDK 21 或更高版本
- **Maven**: 3.6+ （项目已包含 Maven Wrapper，无需单独安装）
- **浏览器**: 现代浏览器（Chrome、Firefox、Edge、Safari 等）

### 推荐工具

- **IDE**: IntelliJ IDEA、VS Code（需安装 Java 扩展）
- **终端**: 任意终端工具

---

## 快速开始

### 1. 启动服务器

#### 方式一：使用 Maven Wrapper（推荐）

```bash
# 在项目根目录执行
./mvnw spring-boot:run
```

#### 方式二：使用已安装的 Maven

```bash
mvn spring-boot:run
```

#### 方式三：打包后运行

```bash
# 打包
./mvnw clean package

# 运行
java -jar target/chat-0.0.1-SNAPSHOT.jar
```

### 2. 确认启动成功

看到以下输出表示启动成功：

```
Started ChatApplication in X.XXX seconds
```

服务器默认运行在 `http://localhost:8080`

### 3. 访问前端

在浏览器中打开：

```
http://localhost:8080
```

### 4. 首次使用

1. **登录界面**：输入用户名和密码
2. **新用户**：首次登录会自动注册，密码会被加密保存
3. **老用户**：需要输入正确的密码

---

## 用户指南

### 登录系统

1. 打开浏览器访问 `http://localhost:8080`
2. 在登录界面输入用户名和密码
3. 点击"登录/注册"按钮
4. 登录成功后自动进入聊天界面

**注意事项**：
- 新用户首次登录会自动注册
- 同一账号在多设备登录时，旧设备会被强制下线
- 用户名为 `admin` 的账号拥有管理员权限

### 私聊功能

#### 发送私聊消息

1. 在左侧"联系人"标签页选择要聊天的用户
2. 在输入框中输入消息内容
3. 点击"发送"按钮或按 Enter 键发送

#### 查看历史消息

- 选择联系人后，会自动加载历史消息
- 向上滚动可查看更多历史消息

#### 撤回消息

1. 在消息上点击"撤回"按钮
2. 只能撤回 2 分钟内发送的消息
3. 撤回后消息会显示为"你撤回了一条消息"或"XXX 撤回了一条消息"

#### 消息反应

1. 在消息上点击"反应"按钮
2. 选择反应类型（点赞、点踩、爱心等）
3. 可以撤销之前的反应

### 群聊功能

#### 创建群组

1. 切换到"群组"标签页
2. 点击"+ 创建群组"按钮
3. 输入群组名称
4. 点击"创建"按钮

#### 加入群组

1. 切换到"在线用户"标签页
2. 找到要邀请的用户
3. 私聊邀请对方加入群组（当前版本需要知道群组 ID）
4. 或使用创建群组时添加初始成员

#### 发送群消息

1. 在"群组"标签页选择要发送消息的群组
2. 在输入框中输入消息内容
3. 点击"发送"按钮发送

#### @ 提醒功能

1. 在群聊界面点击输入框上方的 `@` 按钮
2. 选择要 @ 的成员（或选择"@所有人"，仅群主可见）
3. 输入框中会插入 `@用户名`
4. 被 @ 的用户在左侧群组列表会显示红色"[有人@我]"提醒
5. 点击进入群聊后，提醒会自动清除

**@ 文本删除**：
- 按 Backspace 键可以整体删除 `@用户名`，而不是逐字删除

#### 查看群成员

1. 在群聊界面点击右上角的"成员"图标
2. 查看群成员列表
3. 显示群主和管理员标识：
   - **群主**：黄色"群主"标签
   - **管理员**：绿色"群聊管理员"标签

#### 管理群成员（群主/管理员）

1. 在群成员列表中，点击某个成员旁边的"管理"按钮
2. 选择操作：
   - **禁言**：输入禁言时长（分钟），禁言后用户无法在群内发送消息
   - **踢出群聊**：将成员移出群组
   - **设为管理员**（仅群主）：授予管理员权限
   - **取消管理员**（仅群主）：取消管理员权限

#### 退出/解散群组

- **普通成员**：点击群成员列表底部的"退出群聊"按钮
- **群主**：点击"解散群聊"按钮（群主不能退出，只能解散）

### 在线用户

1. 切换到"在线用户"标签页
2. 查看当前所有在线用户
3. 点击用户可以直接开始私聊
4. 管理员用户会显示 👑 图标

---

## 技术架构

### 后端架构

- **框架**: Spring Boot 3.3.0
- **Java 版本**: 21
- **通信协议**: WebSocket (JSON 格式)
- **数据存储**: 内存存储（ConcurrentHashMap）
- **架构模式**: 
  - 策略模式（ActionHandler 接口）
  - 处理器模式（HandlerRegistry 注册中心）

### 前端架构

- **技术栈**: 纯 HTML + JavaScript（原生，无框架）
- **样式**: CSS3
- **通信**: WebSocket API

### 核心模块

```
com.example.chat/
├── config/              # 配置类
│   ├── WebSocketConfig         # WebSocket 配置
│   └── AuthHandshakeInterceptor # 握手拦截器
├── handler/             # 处理器
│   ├── ChatHandler             # 主消息处理器
│   ├── HandlerRegistry         # 处理器注册中心
│   └── action/impl/            # 具体功能处理器
├── service/             # 业务服务
│   ├── UserService             # 用户服务
│   └── MessageService          # 消息服务
├── repository/          # 数据存储
│   └── DataCenter              # 内存数据中心
└── util/                # 工具类
    └── SensitiveWordFilter     # 敏感词过滤器
```

---

## API 文档

### WebSocket 连接

**连接地址**：
```
ws://localhost:8080/chat
```

### 消息格式

#### 客户端请求格式

```json
{
  "action": "指令类型",
  "params": {
    // 具体参数
  }
}
```

#### 服务端响应格式

```json
{
  "type": "响应类型",
  "code": 200,
  "msg": "ok",
  "data": {
    // 业务数据
  }
}
```

### 支持的 Action 指令

| Action | 说明 | 必需参数 | 权限要求 | 响应类型 |
|--------|------|----------|----------|----------|
| `LOGIN` | 登录/注册 | `username`, `password` | 无 | `LOGIN_RESP` |
| `SEND_PRIVATE` | 发送私聊消息 | `targetUser`, `content` | 已登录 | `SUCCESS` / `EVENT_CHAT_MSG` |
| `SEND_GROUP` | 发送群聊消息 | `groupId`, `content` | 已登录，群成员 | `SUCCESS` / `EVENT_CHAT_MSG` |
| `RECALL_MSG` | 撤回消息 | `msgId` | 发送者 | `SUCCESS` / `EVENT_MSG_RECALLED` |
| `MSG_READ` | 标记已读 | `msgId` | 已登录 | `SUCCESS` / `EVENT_MSG_READ` |
| `MSG_REACT` | 消息反应 | `msgId`, `reactType` | 已登录 | `SUCCESS` / `EVENT_MSG_REACT` |
| `GET_HISTORY` | 获取历史消息 | `targetUser` 或 `groupId`, `beforeTime`(可选) | 已登录 | `SUCCESS` |
| `GET_GROUPS` | 获取群组列表 | 无 | 已登录 | `SUCCESS` |
| `CREATE_GROUP` | 创建群组 | `groupName`, `members`(可选) | 已登录 | `SUCCESS` / `GROUP_CREATED` |
| `JOIN_GROUP` | 加入群组 | `groupId` | 已登录 | `SUCCESS` / `GROUP_JOINED` |
| `LEAVE_GROUP` | 退出群组 | `groupId` | 已登录，群成员 | `SUCCESS` |
| `GET_GROUP_MEMBERS` | 获取群成员 | `groupId` | 已登录，群成员 | `GROUP_MEMBERS_RESP` |
| `MUTE_GROUP_USER` | 禁言群成员 | `groupId`, `targetUser`, `durationMinutes` | 群主/管理员 | `SUCCESS` |
| `KICK_GROUP_USER` | 踢出群成员 | `groupId`, `targetUser` | 群主/管理员 | `SUCCESS` / `GROUP_KICKED` |
| `SET_GROUP_ADMIN` | 设置/取消管理员 | `groupId`, `targetUser`, `action`(set/remove) | 群主 | `SUCCESS` |
| `DISSOLVE_GROUP` | 解散群组 | `groupId` | 群主 | `SUCCESS` / `GROUP_DISSOLVED` |
| `HEARTBEAT` | 心跳包 | 无 | 无 | `HEARTBEAT_RESP` |

### 事件类型

服务端主动推送的事件：

| 事件类型 | 说明 | 触发时机 |
|----------|------|----------|
| `EVENT_CHAT_MSG` | 新消息事件 | 收到新消息时 |
| `EVENT_MSG_RECALLED` | 消息撤回事件 | 消息被撤回时 |
| `EVENT_MSG_READ` | 消息已读事件 | 消息被标记为已读时 |
| `EVENT_MSG_REACT` | 消息反应事件 | 消息收到反应时 |
| `GROUP_CREATED` | 群组创建事件 | 创建群组后广播给所有初始成员 |
| `GROUP_JOINED` | 加入群组事件 | 用户加入群组后 |
| `GROUP_UPDATED` | 群组更新事件 | 群组信息变更时（成员变化、管理员变更等） |
| `GROUP_DISSOLVED` | 群组解散事件 | 群组被解散后广播给所有成员 |
| `GROUP_KICKED` | 被踢出群组事件 | 用户被踢出群组时 |
| `ONLINE_LIST` | 在线用户列表更新 | 用户登录/登出时 |
| `SYS_NOTICE` | 系统通知 | 被强制下线等系统通知 |

### API 示例

#### 1. 登录

**请求**：
```json
{
  "action": "LOGIN",
  "params": {
    "username": "alice",
    "password": "123456"
  }
}
```

**响应**：
```json
{
  "type": "LOGIN_RESP",
  "code": 200,
  "msg": "ok",
  "data": {
    "userId": "alice",
    "username": "alice",
    "role": "USER"
  }
}
```

#### 2. 发送私聊消息

**请求**：
```json
{
  "action": "SEND_PRIVATE",
  "params": {
    "targetUser": "bob",
    "content": "Hello!",
    "atUsers": []
  }
}
```

**响应**（发送者）：
```json
{
  "type": "SUCCESS",
  "code": 200,
  "data": {
    "msgId": "uuid",
    "fromUser": "alice",
    "toUser": "bob",
    "content": "Hello!",
    "timestamp": 1234567890123
  }
}
```

**事件**（接收者）：
```json
{
  "type": "EVENT_CHAT_MSG",
  "data": {
    "msgId": "uuid",
    "fromUser": "alice",
    "toUser": "bob",
    "content": "Hello!",
    "timestamp": 1234567890123
  }
}
```

#### 3. 发送群消息（带 @）

**请求**：
```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "group-uuid",
    "content": "@bob 请查看一下",
    "atUsers": ["bob"]
  }
}
```

#### 4. 创建群组

**请求**：
```json
{
  "action": "CREATE_GROUP",
  "params": {
    "groupName": "开发组",
    "members": ["bob", "charlie"]
  }
}
```

**响应**：
```json
{
  "type": "GROUP_CREATED",
  "code": 200,
  "data": {
    "groupId": "group-uuid",
    "groupName": "开发组",
    "owner": "alice",
    "members": ["alice", "bob", "charlie"]
  }
}
```

#### 5. 禁言群成员

**请求**：
```json
{
  "action": "MUTE_GROUP_USER",
  "params": {
    "groupId": "group-uuid",
    "targetUser": "bob",
    "durationMinutes": 10
  }
}
```

**响应**：
```json
{
  "type": "SUCCESS",
  "code": 200,
  "data": {
    "groupId": "group-uuid",
    "targetUser": "bob",
    "durationMinutes": 10,
    "message": "已禁言用户 bob 10 分钟"
  }
}
```

---

## 常见问题

### 1. 如何修改服务器端口？

编辑 `src/main/resources/application.properties`：

```properties
server.port=8081
```

### 2. 消息历史会永久保存吗？

不会。当前版本使用内存存储，服务器重启后所有数据会丢失。如需持久化，需要集成数据库。

### 3. 如何添加新的敏感词？

编辑 `src/main/java/com/example/chat/util/SensitiveWordFilter.java` 中的敏感词列表，或实现从文件/数据库加载。

### 4. 多端登录是如何工作的？

- 同一账号在新设备登录时，旧设备的 WebSocket 连接会被关闭
- 旧设备会收到 `SYS_NOTICE` 类型的消息，提示"您的账号在另一地点登录，您已被强制下线"

### 5. 消息撤回的时间限制是多少？

当前限制为 **2 分钟**，超过 2 分钟的消息无法撤回。

### 6. 群主和管理员的权限有什么区别？

- **群主**：
  - ✅ 禁言成员
  - ✅ 踢出成员
  - ✅ 设置/取消管理员
  - ✅ 解散群组
  - ❌ 不能退出群组（只能解散）

- **管理员**：
  - ✅ 禁言成员（不能禁言群主和其他管理员）
  - ✅ 踢出成员（不能踢出群主和其他管理员）
  - ❌ 不能设置管理员
  - ❌ 不能解散群组
  - ✅ 可以退出群组

### 7. @ 功能如何使用？

1. 在群聊中点击输入框上方的 `@` 按钮
2. 选择要 @ 的成员
3. 输入框中会自动插入 `@用户名`
4. 按 Backspace 可以整体删除 `@用户名`
5. 被 @ 的用户会在左侧群组列表看到红色提醒

### 8. 如何成为系统管理员？

用户名为 `admin` 的账号自动拥有系统管理员权限，可以：
- 踢出在线用户
- 全局禁言用户

### 9. 连接断开怎么办？

- 前端会自动尝试重连
- 如果连接长时间断开，需要刷新页面重新登录

### 10. 如何查看完整的 API 文档？

可以查看项目中的 `功能分析与测试指南.md` 和 `APIdoc.md` 文件，其中包含更详细的 API 说明和测试用例。

---

## 更新日志

### v1.0.0 (当前版本)

- ✅ 用户认证与权限管理
- ✅ 私聊功能
- ✅ 群聊功能（创建、加入、退出、解散）
- ✅ 消息撤回
- ✅ 消息已读回执
- ✅ 消息反应
- ✅ @ 提醒功能
- ✅ 群成员管理（禁言、踢出、设置管理员）
- ✅ 敏感词过滤
- ✅ 历史消息查询
- ✅ 在线用户列表
- ✅ 现代化前端界面

---

## 技术支持

如有问题或建议，请查看项目文档或提交 Issue。

---

**最后更新**: 2024年

