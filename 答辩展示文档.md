# 实时聊天系统 - 答辩展示文档

## 📋 展示结构（总时长：12-15分钟）

---

## 第一部分：项目概述（2分钟）

### 1.1 项目定位
**展示内容：**
- 这是一个基于 **Spring Boot + WebSocket** 的实时聊天系统
- 支持单聊、群聊、消息撤回、@提醒等完整功能
- 采用**内存存储**，开箱即用，适合学习和演示

**设计思考：**
- 为什么选择 Spring Boot？
  - Spring Boot 提供了开箱即用的 WebSocket 支持
  - 自动配置减少了大量样板代码
  - 依赖注入和面向接口编程使代码结构清晰
  
- 为什么使用 WebSocket？
  - HTTP 轮询效率低，延迟高
  - WebSocket 提供**全双工通信**，消息实时推送
  - 减少服务器压力（无需频繁轮询）

**关键数据：**
- 后端：Spring Boot 3.3.0 + Java 21
- 前端：原生 HTML/CSS/JavaScript（无框架依赖）
- 核心框架代码：约 350 行
- Handler 处理器：22 个

---

## 第二部分：技术架构设计（3-4分钟）

### 2.1 整体架构图

```
客户端（浏览器）
    ↕ WebSocket (JSON)
ChatHandler (入口)
    ↕ 路由
HandlerRegistry (注册中心)
    ↕ 分发
ActionHandler (处理器) → Service (业务逻辑) → DataCenter (数据存储)
```

### 2.2 核心设计模式：HandlerRegistry 策略模式

**展示代码位置：**
- `HandlerRegistry.java` - 注册中心
- `ChatHandler.java` - 路由逻辑
- `ActionHandler.java` - 策略接口

**设计思考：**

1. **为什么采用 HandlerRegistry 模式？**

   **问题背景：**
   - 传统方式：在 ChatHandler 中用大 switch-case 处理所有 action
   - 问题：代码臃肿、难以扩展、违反开闭原则

   **解决方案：**
   ```java
   // ChatHandler.java 中的关键代码
   ActionHandler handler = handlerRegistry.getHandler(request.getAction());
   if (handler != null) {
       handler.handle(session, request);
   }
   ```
   
   **优势：**
   - ✅ **高扩展性**：新增功能只需添加新的 Handler，无需修改现有代码
   - ✅ **职责分离**：每个 Handler 只负责一个功能，代码清晰
   - ✅ **易于测试**：每个 Handler 可以独立测试
   - ✅ **自动注册**：通过 Spring 的 `@PostConstruct` 自动发现并注册所有 Handler

2. **BaseActionHandler 的设计思路**

   **代码位置：** `BaseActionHandler.java`
   
   **设计思考：**
   - 提取**公共方法**：发送响应、错误处理、权限检查
   - 减少代码重复：所有 Handler 继承此基类
   - 统一错误处理：确保所有 Handler 的错误处理逻辑一致

3. **数据存储设计：DataCenter**

   **代码位置：** `DataCenter.java`
   
   **设计思考：**
   ```java
   public static final Map<String, WebSocketSession> ONLINE_USERS = new ConcurrentHashMap<>();
   public static final Map<String, User> USERS = new ConcurrentHashMap<>();
   public static final Map<String, Group> GROUPS = new ConcurrentHashMap<>();
   public static final Map<String, Message> MSG_HISTORY = new ConcurrentHashMap<>();
   ```
   
   **为什么使用 ConcurrentHashMap？**
   - WebSocket 是多线程环境，需要线程安全
   - ConcurrentHashMap 提供高性能的并发访问
   - 比 synchronized 更细粒度的锁，性能更好
   
   **为什么使用内存存储？**
   - 项目定位：学习/演示项目，追求快速开发和部署
   - 性能优势：内存访问速度快，适合高并发场景
   - 简化架构：无需配置数据库，降低复杂度
   - **未来扩展**：可以轻松替换为数据库存储（只需修改 Repository 层）

### 2.3 性能优化：线程池异步处理

**代码位置：** `AppConfig.java`, `ChatHandler.java`

**设计思考：**

1. **为什么使用线程池？**
   ```java
   @Bean(name = "chatExecutor")
   public Executor chatExecutor() {
       ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
       executor.setCorePoolSize(10);      // 核心线程数
       executor.setMaxPoolSize(20);       // 最大线程数
       executor.setQueueCapacity(200);    // 队列容量
       return executor;
   }
   ```
   
   - **消息发送是耗时操作**：需要过滤敏感词、路由到多个用户、序列化 JSON
   - **异步处理**：不阻塞 WebSocket 主线程，提高并发能力
   - **资源控制**：通过线程池限制并发数，防止资源耗尽

2. **哪些操作异步，哪些同步？**
   
   ```java
   // ChatHandler.java
   if ("SEND_PRIVATE".equals(request.getAction()) || "SEND_GROUP".equals(request.getAction())) {
       threadPool.execute(() -> handler.handle(session, request));  // 异步
   } else {
       handler.handle(session, request);  // 同步
   }
   ```
   
   - **异步**：SEND_PRIVATE, SEND_GROUP（耗时，需要广播）
   - **同步**：LOGIN, GET_FRIENDS 等（快速查询，需要立即返回）

---

## 第三部分：核心功能展示（5-6分钟）

### 3.1 用户认证与权限管理

**展示顺序：**
1. 新用户自动注册
2. 已存在用户密码验证（BCrypt 加密）
3. 多端顶号功能

**技术要点讲解：**

**为什么使用 BCrypt 加密？**
- 代码位置：`UserServiceImpl.java`
- 安全性：BCrypt 是专门为密码设计的哈希算法
- 不可逆：即使数据库泄露，也无法还原原始密码
- 自适应：可以通过增加轮数提高安全性

**多端顶号实现思路：**
```java
// LoginHandler.java 中的关键逻辑
WebSocketSession oldSession = DataCenter.ONLINE_USERS.get(username);
if (oldSession != null && oldSession.isOpen()) {
    oldSession.close();  // 关闭旧连接
}
```

### 3.2 实时消息通信

**展示顺序：**
1. 私聊消息发送与接收
2. 群聊消息广播
3. 消息历史查询

**技术要点讲解：**

**消息路由机制：**
- 私聊：根据 `toUser` 查找目标用户的 WebSocket Session，直接推送
- 群聊：遍历群组所有成员，批量推送消息

**消息存储结构：**
```java
Message {
    msgId: String          // UUID 保证唯一性
    fromUser: String       
    toUser: String         // 私聊是用户名，群聊是GroupId
    isGroup: boolean       
    content: String        
    timestamp: Long        // 13位毫秒时间戳
    atUsers: List<String>  // @的用户列表
}
```

### 3.3 @提醒功能

**展示顺序：**
1. 普通成员 @ 特定用户
2. 群主 @ 所有人

**技术要点讲解：**

**前端解析：**
- 前端通过正则表达式识别 `@用户名` 格式
- 将解析出的用户名列表传给后端

**后端处理：**
- 存储 `atUsers` 列表
- 被 @ 的用户收到特殊通知
- 前端高亮显示 @ 的内容

### 3.4 消息撤回功能

**展示顺序：**
1. 发送消息
2. 在 2 分钟内撤回
3. 超过 2 分钟无法撤回（演示错误提示）

**技术要点讲解：**

**时间限制实现：**
```java
// Recall_MsgHandler.java
long recallLimitMs = 120000; // 2分钟
long timeDiff = System.currentTimeMillis() - message.getTimestamp();
if (timeDiff > recallLimitMs) {
    sendError(session, "消息已超过2分钟，无法撤回");
    return;
}
```

**撤回后的同步：**
- 私聊：通知发送者和接收者
- 群聊：广播给所有群成员
- 使用 `EVENT_MSG_RECALLED` 事件类型

### 3.5 敏感词过滤（重点技术）

**代码位置：** `SensitiveWordFilter.java`

**设计思考：**

1. **为什么选择 AC 自动机算法？**

   **问题背景：**
   - 系统需要同时匹配多个敏感词（20个）
   - 暴力匹配效率低：O(n×m)，n为文本长度，m为敏感词数量

   **AC 自动机优势：**
   - 时间复杂度：O(n+m)，只需一次遍历文本
   - 多模式匹配：一次扫描匹配所有敏感词
   - 适合敏感词过滤场景

2. **算法原理（简要说明）：**
   ```
   Trie树（前缀树）
   + 失败指针（类似KMP的next数组）
   = AC自动机
   ```
   
   - **构建 Trie 树**：将所有敏感词构建成树
   - **构建失败指针**：当匹配失败时，跳转到最长公共后缀
   - **匹配过程**：一次遍历文本，通过失败指针避免重复匹配

3. **实现细节：**
   ```java
   // 核心匹配逻辑
   TrieNode node = root;
   for (char c : text.toCharArray()) {
       // 沿着失败指针跳转
       while (node != root && !node.children.containsKey(c)) {
           node = node.fail;
       }
       // 移动到下一个节点
       if (node.children.containsKey(c)) {
           node = node.children.get(c);
       }
       // 检查是否匹配到敏感词
       if (node.isEnd) {
           // 标记需要替换的位置
       }
   }
   ```

**演示要点：**
- 输入包含敏感词的文本
- 展示过滤后的结果（敏感词被替换为 *）
- 强调算法的效率优势

### 3.6 权限管理

**展示顺序：**
1. 群主权限：禁言、踢人、设置管理员、解散群组
2. 管理员权限：禁言、踢人（不能操作群主）
3. 权限校验失败提示

**技术要点讲解：**

**权限校验设计：**
```java
// BaseActionHandler.java
protected boolean isAdmin(String username) {
    User user = DataCenter.USERS.get(username);
    return user != null && user.isAdmin();
}
```

**三层权限体系：**
1. **系统管理员（admin）**：全局权限
2. **群主（owner）**：群组内最高权限
3. **群管理员（admin）**：部分群组权限
4. **普通成员**：基础权限

**权限校验位置：**
- 在 Handler 中统一校验
- 使用 `sendError` 返回权限不足提示

---

## 第四部分：技术亮点总结（2-3分钟）

### 4.1 架构设计亮点

1. **HandlerRegistry 模式**
   - 高扩展性、低耦合
   - 符合开闭原则

2. **BaseActionHandler 抽象**
   - 代码复用
   - 统一错误处理

3. **线程池异步处理**
   - 提高并发能力
   - 合理利用系统资源

### 4.2 算法亮点

1. **AC 自动机算法**
   - O(n+m) 时间复杂度
   - 多模式匹配

2. **BCrypt 密码加密**
   - 安全性高
   - 不可逆哈希

### 4.3 工程实践

1. **线程安全设计**
   - ConcurrentHashMap
   - synchronized 保护关键区域

2. **错误处理机制**
   - 统一的错误响应格式
   - 友好的错误提示

---

## 第五部分：项目总结与展望（1分钟）

### 5.1 已完成功能

- ✅ 用户认证与权限管理
- ✅ 私聊、群聊
- ✅ 消息撤回、@提醒
- ✅ 敏感词过滤
- ✅ 权限管理（群主/管理员）

### 5.2 技术指标

- 核心框架代码：约 350 行
- Handler 处理器：22 个
- 支持功能：20+ 个 WebSocket Action

### 5.3 可改进方向

1. **数据持久化**
   - 当前：内存存储，重启数据丢失
   - 改进：引入 MySQL/Redis，实现数据持久化

2. **消息队列**
   - 当前：直接推送
   - 改进：引入 RabbitMQ/Kafka，解耦消息发送

3. **分布式支持**
   - 当前：单机部署
   - 改进：多节点部署，共享状态（Redis）

4. **安全性增强**
   - JWT Token 认证
   - HTTPS/WSS 加密传输
   - 更细粒度的权限控制

5. **性能优化**
   - 消息分页加载
   - 图片/文件上传
   - 消息推送（离线消息）

---

## 📝 答辩技巧提示

### 展示顺序建议

1. **先架构后功能**
   - 让评委理解整体设计
   - 功能演示时有技术背景支撑

2. **边演示边讲解**
   - 不要只演示不说话
   - 每个功能演示时说明技术实现

3. **突出技术亮点**
   - HandlerRegistry 模式（架构设计）
   - AC 自动机算法（算法能力）
   - 线程池优化（性能意识）

4. **承认不足，展示思考**
   - 说明内存存储的局限性
   - 提出改进方向，展示学习能力

### 可能的问题应对

**Q: 为什么不用数据库？**
A: 这是学习和演示项目，内存存储简化了架构，便于快速开发和理解。但我们也考虑了扩展性，可以轻松替换为数据库存储，只需修改 Repository 层。

**Q: 如何处理高并发？**
A: 使用了线程池异步处理消息发送，避免阻塞主线程。使用了 ConcurrentHashMap 保证线程安全。如果进一步优化，可以引入消息队列和 Redis 缓存。

**Q: 数据丢失怎么办？**
A: 当前是内存存储，重启会丢失数据。这是项目定位的权衡。在生产环境中，我们会引入数据库持久化，并通过 Redis 实现缓存层。

**Q: 这个项目的工作量有多少？**
A: 核心框架代码约 350 行，22 个 Handler 处理器，完整的前后端实现。重点在于架构设计和代码质量，而不是代码行数。

---

## 🎯 答辩时间分配参考

| 部分 | 时间 | 重点 |
|------|------|------|
| 项目概述 | 2分钟 | 技术选型理由 |
| 架构设计 | 3-4分钟 | HandlerRegistry 模式、线程池 |
| 功能演示 | 5-6分钟 | 重点演示敏感词过滤、@提醒 |
| 技术总结 | 2-3分钟 | AC 自动机算法 |
| 总结展望 | 1分钟 | 改进方向 |
| **总计** | **13-16分钟** | |

---

## ✅ 答辩前检查清单

- [ ] 项目能正常运行（多个浏览器窗口测试）
- [ ] 准备好3-4个测试账号
- [ ] 关键代码文件位置熟悉（HandlerRegistry, SensitiveWordFilter）
- [ ] 功能演示流程熟悉（按顺序不卡顿）
- [ ] 技术要点能流畅讲解
- [ ] 准备好回答常见问题

---

**祝答辩顺利！** 🎉

