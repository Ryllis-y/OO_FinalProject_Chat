# 聊天系统功能分析与测试指南

## 📋 系统概览

这是一个基于 **Spring Boot + WebSocket** 的实时聊天系统，使用**内存存储**（不依赖数据库），支持单聊、群聊、消息撤回、已读回执、@提醒等高级功能。

### 技术栈
- **后端框架**: Spring Boot 3.3.0
- **Java版本**: 21
- **通信协议**: WebSocket (JSON 格式)
- **数据存储**: 内存 (ConcurrentHashMap)
- **架构模式**: 策略模式 + 处理器模式

---

## 🎯 核心功能模块

### 1. 用户认证与权限管理

#### 功能点
- ✅ **自动注册**: 新用户首次登录自动注册（无需密码验证）
- ✅ **密码验证**: 已存在用户需要验证密码（使用 BCrypt 加密）
- ✅ **多端顶号**: 同一账号在另一地点登录时，旧连接会被强制下线
- ✅ **管理员权限**: 用户名为 `admin` 的账号自动拥有管理员权限
- ✅ **在线状态**: 实时维护在线用户列表

#### 相关类
- `LoginHandler`: 处理登录逻辑
- `UserService`: 用户业务逻辑（注册、验证、加密）
- `DataCenter.ONLINE_USERS`: 在线用户会话映射
- `DataCenter.USERS`: 用户信息存储

---

### 2. 消息发送与接收

#### 功能点
- ✅ **私聊消息** (`SEND_PRIVATE`): 点对点消息发送
- ✅ **群聊消息** (`SEND_GROUP`): 群组消息广播
- ✅ **@提醒**: 支持 @ 特定用户，后端解析并透传
- ✅ **消息路由**: 基于线程池异步处理，支持高并发
- ✅ **消息持久化**: 消息存储在内存中，支持历史查询

#### 消息数据结构
```java
Message {
    msgId: String          // 消息ID（UUID）
    fromUser: String       // 发送者
    toUser: String         // 接收者（私聊是用户名，群聊是GroupId）
    isGroup: boolean       // 是否群聊
    content: String        // 消息内容
    timestamp: Long        // 时间戳（13位毫秒）
    quoteId: String        // 引用回复的消息ID
    quoteContent: String   // 引用回复的内容
    atUsers: List<String>  // @的用户列表
    readBy: Set<String>    // 已读用户列表
    reactions: Map         // 表情反应（点赞/点踩等）
}
```

#### 相关类
- `Send_PrivateHandler`: 私聊消息处理器
- `MessageService`: 消息业务逻辑（创建、保存、路由）
- `DataCenter.MSG_HISTORY`: 消息历史存储

---

### 3. 消息交互功能

#### 3.1 消息撤回 (`RECALL_MSG`)
- ✅ 支持撤回 2 分钟内发送的消息
- ✅ 撤回后所有端同步删除该消息
- ✅ 发送撤回通知给相关用户

#### 3.2 已读回执 (`MSG_READ`)
- ✅ 标记消息为已读
- ✅ 实时更新消息的已读列表
- ✅ 发送者可以查看谁读了消息

#### 3.3 消息反应 (`MSG_REACT`)
- ✅ 支持对消息进行点赞/点踩等反应
- ✅ 实时更新反应计数
- ✅ 支持撤销反应

#### 相关类
- `Recall_MsgHandler`: 消息撤回处理器
- `Msg_ReadHandler`: 已读回执处理器
- `Msg_ReactHandler`: 消息反应处理器

---

### 4. 历史消息查询

#### 功能点
- ✅ **历史查询** (`GET_HISTORY`): 支持游标翻页获取历史消息
- ✅ **登录推送**: 登录成功后自动推送最近 20 条未读消息（未实现）
- ✅ **消息过滤**: 根据聊天对象（用户/群组）过滤消息

#### 相关类
- `Get_HistoryHandler`: 历史消息查询处理器

---

### 5. 管理员功能

#### 功能点
- ✅ **踢人** (`KICK_USER`): 管理员可以强制踢出在线用户
- ✅ **禁言** (`MUTE_USER`): 管理员可以禁言用户（指定时长）
- ✅ **权限检查**: 自动检查操作者是否为管理员

---

### 6. 心跳保持 (`HEARTBEAT`)
- ✅ 客户端定期发送心跳包保持连接
- ✅ 服务端不做处理（主要为了保持连接不断）

---

## 🌐 WebSocket 连接信息

### 连接地址
```
ws://localhost:8080/chat
```

### 服务器端口
- 默认端口: **8080**
- 配置位置: `src/main/resources/application.properties`

### 跨域配置
- 当前配置: 允许所有来源 (`setAllowedOrigins("*")`)
- 生产环境建议限制特定域名

---

## 📨 消息协议格式

### 客户端发送 (Request)
```json
{
  "action": "指令类型",
  "params": {
    // 具体参数
  }
}
```

### 服务端响应 (Response)
```json
{
  "type": "响应类型",
  "code": 200,
  "msg": "ok",
  "data": {
    // 业务数据
  }
}
```

---

## 📝 支持的 Action 指令

| Action | 说明 | 必需参数 | 权限要求 |
|--------|------|----------|----------|
| `LOGIN` | 登录/注册 | `username`, `password` | 无 |
| `SEND_PRIVATE` | 发送私聊消息 | `targetUser`, `content` | 已登录 |
| `SEND_GROUP` | 发送群聊消息 | `groupId`, `content` | 已登录 |
| `RECALL_MSG` | 撤回消息 | `msgId` | 发送者 |
| `MSG_READ` | 标记已读 | `msgId` | 已登录 |
| `MSG_REACT` | 消息反应 | `msgId`, `reaction` | 已登录 |
| `GET_HISTORY` | 获取历史消息 | `targetUser` 或 `groupId` | 已登录 |
| `GET_ONLINE` | 获取在线用户列表 | 无 | 已登录 |
| `KICK_USER` | 踢出用户 | `targetUser` | 管理员 |
| `MUTE_USER` | 禁言用户 | `targetUser`, `duration` | 管理员 |
| `HEARTBEAT` | 心跳包 | 无 | 无 |

---

## 🧪 测试指南

### 前置准备

1. **启动服务器**
   ```bash
   ./mvnw spring-boot:run
   ```
   
   看到以下输出表示启动成功：
   ```
   Started ChatApplication in X.XXX seconds
   ```

2. **准备测试工具**
   - 浏览器开发者工具（Console）
   - WebSocket 测试工具（如 Postman、websocket.org 的 Echo Test）
   - 或使用 Node.js 测试脚本

---

### 测试用例 1: 用户登录

#### 步骤 1: 建立 WebSocket 连接
```javascript
const ws = new WebSocket('ws://localhost:8080/chat');

ws.onopen = () => {
  console.log('连接已建立');
};

ws.onmessage = (event) => {
  console.log('收到消息:', JSON.parse(event.data));
};
```

#### 步骤 2: 发送登录请求（新用户自动注册）
```javascript
ws.send(JSON.stringify({
  action: "LOGIN",
  params: {
    username: "alice",
    password: "123456"
  }
}));
```

#### 预期响应
```json
{
  "type": "LOGIN_RESP",
  "code": 200,
  "msg": "ok",
  "data": {
    "userId": "...",
    "username": "alice",
    "role": "USER",
    // password 字段不会返回（@JsonIgnore）
    ...
  }
}
```

#### 步骤 3: 测试已存在用户登录（验证密码）
```javascript
// 再次使用相同用户名登录，需要正确密码
ws.send(JSON.stringify({
  action: "LOGIN",
  params: {
    username: "alice",
    password: "123456"  // 正确密码
  }
}));
```

#### 步骤 4: 测试错误密码
```javascript
ws.send(JSON.stringify({
  action: "LOGIN",
  params: {
    username: "alice",
    password: "wrong_password"
  }
}));
```

#### 预期响应
```json
{
  "type": "ERROR",
  "code": 500,
  "msg": "密码错误"
}
```

---

### 测试用例 2: 私聊消息

#### 前提条件
- 两个用户都已登录（Alice 和 Bob）

#### 步骤 1: Alice 发送消息给 Bob
```javascript
ws.send(JSON.stringify({
  action: "SEND_PRIVATE",
  params: {
    targetUser: "bob",
    content: "Hello Bob!",
    atUsers: []  // 可选：@用户列表
  }
}));
```

#### 预期结果
- Alice 收到成功响应（包含完整的 Message 对象）
- Bob 收到 `EVENT_CHAT_MSG` 类型的消息通知

#### 步骤 2: 测试 @提醒
```javascript
ws.send(JSON.stringify({
  action: "SEND_PRIVATE",
  params: {
    targetUser: "bob",
    content: "Hey @bob, are you there?",
    atUsers: ["bob"]
  }
}));
```

#### 步骤 3: 测试目标用户不在线
```javascript
ws.send(JSON.stringify({
  action: "SEND_PRIVATE",
  params: {
    targetUser: "nonexistent",
    content: "This should fail"
  }
}));
```

#### 预期响应
```json
{
  "type": "ERROR",
  "code": 500,
  "msg": "用户 nonexistent 不在线"
}
```

---

### 测试用例 3: 消息撤回

#### 前提条件
- 已发送一条消息（记录 msgId）

#### 步骤 1: 撤回消息
```javascript
ws.send(JSON.stringify({
  action: "RECALL_MSG",
  params: {
    msgId: "刚才发送的消息ID"
  }
}));
```

#### 预期结果
- 发送者和接收者都收到撤回通知
- 消息从历史记录中删除

#### 步骤 2: 测试超时撤回（超过 2 分钟）
- 等待 2 分钟后尝试撤回
- 应该返回错误提示

---

### 测试用例 4: 已读回执

#### 步骤 1: Bob 标记消息为已读
```javascript
ws.send(JSON.stringify({
  action: "MSG_READ",
  params: {
    msgId: "alice发送的消息ID"
  }
}));
```

#### 预期结果
- Bob 收到成功响应
- Alice 应该收到已读状态更新（如果实现了推送）

---

### 测试用例 5: 消息反应

#### 步骤 1: 对消息点赞
```javascript
ws.send(JSON.stringify({
  action: "MSG_REACT",
  params: {
    msgId: "消息ID",
    reaction: "like"  // 或 "dislike"
  }
}));
```

#### 步骤 2: 撤销反应
```javascript
ws.send(JSON.stringify({
  action: "MSG_REACT",
  params: {
    msgId: "消息ID",
    reaction: "like",  // 相同的反应类型
    remove: true  // 撤销
  }
}));
```

---

### 测试用例 6: 获取历史消息

#### 步骤 1: 获取与某用户的聊天历史
```javascript
ws.send(JSON.stringify({
  action: "GET_HISTORY",
  params: {
    targetUser: "bob",
    limit: 20,  // 可选：每页数量
    before: null  // 可选：游标（获取此消息之前的消息）
  }
}));
```

#### 预期响应
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "messages": [...],  // 消息列表
    "hasMore": true,    // 是否还有更多
    "nextCursor": "..." // 下一页游标
  }
}
```

---

### 测试用例 7: 管理员功能

#### 步骤 1: 管理员登录
```javascript
ws.send(JSON.stringify({
  action: "LOGIN",
  params: {
    username: "admin",
    password: "admin123"  // 首次登录会自动注册
  }
}));
```

#### 步骤 2: 管理员踢人
```javascript
ws.send(JSON.stringify({
  action: "KICK_USER",
  params: {
    targetUser: "alice"
  }
}));
```

#### 预期结果
- Alice 的连接被强制关闭
- Alice 收到被踢通知

#### 步骤 3: 管理员禁言
```javascript
ws.send(JSON.stringify({
  action: "MUTE_USER",
  params: {
    targetUser: "bob",
    duration: 3600000  // 1小时（毫秒）
  }
}));
```

#### 步骤 4: 测试被禁言用户发送消息
- Bob 尝试发送消息应该被拒绝

---

### 测试用例 8: 多端顶号

#### 步骤 1: 同一账号在浏览器 A 登录
```javascript
const ws1 = new WebSocket('ws://localhost:8080/chat');
ws1.send(JSON.stringify({
  action: "LOGIN",
  params: { username: "alice", password: "123456" }
}));
```

#### 步骤 2: 同一账号在浏览器 B 登录
```javascript
const ws2 = new WebSocket('ws://localhost:8080/chat');
ws2.send(JSON.stringify({
  action: "LOGIN",
  params: { username: "alice", password: "123456" }
}));
```

#### 预期结果
- ws1 收到被顶号通知并自动断开
- ws2 成功登录

---

### 测试用例 9: 获取在线用户列表

#### 步骤
```javascript
ws.send(JSON.stringify({
  action: "GET_ONLINE",
  params: {}
}));
```

#### 预期响应
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "users": ["alice", "bob", "admin"]
  }
}
```

---

## 🐛 常见问题排查

### 1. 连接失败
- ✅ 检查服务器是否启动
- ✅ 检查端口 8080 是否被占用
- ✅ 检查防火墙设置

### 2. 消息发送失败
- ✅ 确认已登录（session 中有 username）
- ✅ 检查参数格式是否正确
- ✅ 查看服务器控制台错误日志

### 3. 找不到用户
- ✅ 确认用户已登录（在 ONLINE_USERS 中）
- ✅ 检查用户名拼写是否正确

### 4. 权限错误
- ✅ 管理员功能需要用户名为 `admin`
- ✅ 确认用户已登录

---

## 📊 性能测试建议

### 并发测试
1. **多用户同时连接**: 使用压力测试工具（如 JMeter）模拟多用户连接
2. **消息吞吐量**: 测试每秒能处理多少条消息
3. **内存使用**: 监控内存增长（因为使用内存存储）

### 建议的测试指标
- 支持的最大并发连接数
- 消息延迟（发送到接收的时间）
- 内存占用（随着消息数量增长）

---

## 🔧 测试工具推荐

### 1. 浏览器 Console（最简单）
直接在浏览器控制台运行 JavaScript 代码测试

### 2. Postman
- 支持 WebSocket 连接测试
- 可以保存测试用例

### 3. websocket.org 的 Echo Test
- 在线工具，适合快速测试连接

### 4. Node.js 测试脚本
可以编写自动化测试脚本，批量测试各种场景

---

## 📌 注意事项

1. **数据持久化**: 当前使用内存存储，服务器重启后数据会丢失
2. **并发安全**: 使用了 ConcurrentHashMap，理论上线程安全
3. **生产环境**: 需要添加数据库持久化、消息队列、负载均衡等
4. **安全加固**: 
   - 限制消息长度
   - 防止 SQL 注入（虽然不用数据库，但要防止 XSS）
   - 添加频率限制（防止刷屏）

---

## ✅ 测试检查清单

- [ ] 用户登录/注册
- [ ] 私聊消息发送/接收
- [ ] 消息撤回（2分钟内）
- [ ] 已读回执
- [ ] 消息反应（点赞/点踩）
- [ ] 历史消息查询
- [ ] 管理员踢人
- [ ] 管理员禁言
- [ ] 多端顶号
- [ ] 获取在线用户列表
- [ ] 心跳保持连接
- [ ] 错误处理（错误密码、用户不存在等）
- [ ] 边界情况（空消息、超长消息等）

---

祝测试顺利！🚀

