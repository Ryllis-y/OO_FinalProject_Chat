# 前端使用说明

## 📁 文件位置

前端文件位于：`src/main/resources/static/`
- `index.html` - 主页面
- `style.css` - 样式文件
- `app.js` - JavaScript 核心逻辑

## 🚀 启动方式

1. **启动后端服务器**：
   ```bash
   ./mvnw spring-boot:run
   ```

2. **访问前端**：
   在浏览器中打开：`http://localhost:8080/`
   
   或者直接访问：`http://localhost:8080/index.html`

## ✨ 功能特性

### 1. 用户登录/注册
- 输入用户名和密码即可登录
- 首次登录自动注册
- `admin` 账号自动获得管理员权限

### 2. 私聊功能
- 在"联系人"标签页查看在线用户
- 点击联系人开始私聊
- 支持发送文本消息
- 实时接收消息

### 3. 群聊功能
- 在"群组"标签页查看所有群组
- 创建群组：点击"+ 创建群组"按钮
- 加入群组：通过群组ID加入（需要实现加入群组弹窗）
- 发送群聊消息
- 支持 @ 提醒功能
- 查看群成员列表

### 4. 消息功能
- **发送消息**：在输入框输入内容，按 Enter 或点击"发送"按钮
- **撤回消息**：鼠标悬停在消息上，点击"撤回"按钮（仅2分钟内可撤回）
- **消息反应**：鼠标悬停在消息上，点击"反应"按钮，选择反应类型（👍 👎 ❤️ 😂 😢 😠）
- **已读回执**：私聊消息显示已读状态
- **历史消息**：自动加载历史消息

### 5. 管理员功能
- 管理员可以踢出用户
- 管理员可以禁言用户（需要前端实现）

## 🎨 界面说明

### 左侧边栏
- **用户信息**：显示当前登录用户信息
- **标签页**：
  - 联系人：显示所有联系人
  - 群组：显示所有群组
  - 在线用户：显示所有在线用户
- **搜索框**：搜索联系人/群组

### 中间聊天区域
- **聊天头部**：显示当前聊天对象信息
- **消息列表**：显示所有消息
- **输入区域**：输入和发送消息

### 消息显示
- **自己发送的消息**：右侧显示，蓝色背景
- **他人发送的消息**：左侧显示，白色背景
- **消息操作**：鼠标悬停显示"撤回"和"反应"按钮
- **消息反应**：显示在消息下方的反应列表

## 🔧 使用技巧

1. **发送消息**：
   - 直接输入内容按 Enter 发送
   - Shift + Enter 换行

2. **@ 提醒**（仅群聊）：
   - 点击输入框上方的 @ 按钮
   - 或直接输入 @ 用户名

3. **查看群成员**：
   - 在群聊中点击右上角的"👥"按钮

4. **创建群组**：
   - 点击"群组"标签页的"+ 创建群组"按钮
   - 输入群组名称
   - 可选：输入初始成员（用逗号分隔用户名）

## 📝 注意事项

1. **多端登录**：
   - 同一账号在不同设备登录时，旧设备会被强制下线

2. **敏感词过滤**：
   - 后端会自动过滤敏感词，敏感词会被替换为 `*`

3. **消息撤回**：
   - 只能在发送后2分钟内撤回
   - 只有发送者、管理员或群主可以撤回消息

4. **禁言功能**：
   - 被禁言的用户无法发送消息
   - 会显示剩余禁言时间

5. **连接状态**：
   - 页面会自动保持 WebSocket 连接
   - 如果连接断开，会自动重连

## 🐛 常见问题

### Q: 页面无法打开？
A: 确保后端服务器已启动，访问 `http://localhost:8080/`

### Q: 无法发送消息？
A: 
- 确保已选择聊天对象
- 检查是否被禁言
- 查看浏览器控制台是否有错误信息

### Q: 消息发送失败？
A: 
- 检查 WebSocket 连接状态
- 查看浏览器控制台的错误信息
- 确保消息内容不为空

### Q: 历史消息没有加载？
A: 
- 刷新页面重新加载
- 检查后端是否返回了历史消息数据

## 🎯 后续优化建议

1. **UI 优化**：
   - 添加更多动画效果
   - 优化移动端显示
   - 添加表情选择器

2. **功能增强**：
   - 实现文件上传
   - 添加语音消息
   - 实现消息转发

3. **体验优化**：
   - 添加消息发送状态（发送中、已送达、已读）
   - 实现打字提示
   - 添加消息搜索功能

4. **管理功能**：
   - 实现完整的群组管理界面
   - 添加用户管理界面
   - 实现禁言管理界面

