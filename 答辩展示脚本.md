# 实时聊天系统 - 答辩展示脚本

> 本文档是完整的答辩口头表达脚本，包含所有讲解内容

---

## 【开场】（30秒）

**（面向评委，微笑）**

各位老师好！我今天要展示的项目是《基于 Spring Boot 和 WebSocket 的实时聊天系统》。

这是一个完整的即时通讯应用，支持单聊、群聊、消息撤回、@提醒等功能。项目采用了 HandlerRegistry 策略模式作为核心架构，并使用 AC 自动机算法实现敏感词过滤。

接下来我将从**项目概述、架构设计、功能演示**三个方面进行展示。

---

## 第一部分：项目概述（2分钟）

### 1.1 项目定位与技术选型

**（切换到PPT或项目介绍页面）**

首先，让我介绍一下这个项目的定位。

这是一个**实时聊天系统**，主要特点包括：

第一，采用 **Spring Boot 3.3.0** 作为后端框架。选择 Spring Boot 是因为它提供了开箱即用的 WebSocket 支持，大大简化了开发工作。而且，它的自动配置和依赖注入机制，让代码结构更加清晰。

第二，通信协议选择 **WebSocket**。这里我想说明一下为什么不用传统的 HTTP 轮询。HTTP 轮询需要客户端不断向服务器发送请求，这会导致两个问题：一是延迟高，消息不能实时推送；二是服务器压力大。而 WebSocket 提供了**全双工通信**，服务器可以主动推送消息给客户端，实现了真正的实时通信。

第三，数据存储采用**内存存储**，使用 ConcurrentHashMap。这是考虑到项目的定位——作为一个学习和演示项目，内存存储可以让系统开箱即用，无需配置数据库，降低了部署复杂度。当然，我也考虑了扩展性，未来可以轻松替换为数据库存储。

**（切换代码统计）**

从代码规模来看，核心框架代码约 **350 行**，包含了 22 个 Handler 处理器，实现了 20 多个 WebSocket Action。虽然代码量不大，但重点在于**架构设计和代码质量**。

---

## 第二部分：技术架构设计（3-4分钟）

### 2.1 整体架构

**（展示架构图或打开 IDE 展示项目结构）**

让我来展示一下系统的整体架构。

**（指向架构图或代码结构）**

整个系统分为几个层次：

**最上层是客户端**，使用原生的 HTML、CSS、JavaScript，没有引入任何前端框架，这样可以保持项目的轻量化。

**中间是 WebSocket 通信层**，所有的数据交互都通过 JSON 格式进行传输。

**核心是 ChatHandler**，这是 WebSocket 消息的入口，所有消息都会先到达这里。

**接下来是 HandlerRegistry**，这是系统的**注册中心**，负责将不同的 Action 路由到对应的处理器。

**最后是具体的 Handler**，每个 Handler 负责处理一个具体的功能，比如登录、发送消息、创建群组等。

### 2.2 HandlerRegistry 模式（重点讲解）

**（打开 HandlerRegistry.java 文件）**

接下来我要重点讲解一下 **HandlerRegistry 模式**，这是这个项目最核心的设计。

**（指向代码）**

首先，让我看看传统的做法。如果我们用传统的 switch-case 方式，ChatHandler 会变得非常臃肿，可能有几百行的 switch 语句，每新增一个功能就要修改 ChatHandler，这违反了开闭原则。

**（指向 HandlerRegistry 代码）**

所以我采用了 **HandlerRegistry 模式**，这是一个策略模式的变体。

**（朗读关键代码）**

```java
@PostConstruct
public void init() {
    actionHandlerBeans.forEach((beanName, handler) -> {
        String action = beanName.replace("Handler", "").toUpperCase();
        registerHandler(action, handler);
    });
}
```

这个注册中心在系统启动时，会自动发现所有实现了 ActionHandler 接口的 Bean，并将它们注册到 Map 中。action 的名称是根据 Bean 的名称自动转换的，比如 `LoginHandler` 对应 `LOGIN` action。

**（切换到 ChatHandler.java）**

然后在 ChatHandler 中，路由变得非常简单：

**（指向代码）**

```java
ActionHandler handler = handlerRegistry.getHandler(request.getAction());
if (handler != null) {
    handler.handle(session, request);
}
```

只需要一行代码就能完成路由，非常优雅。

**（讲解优势）**

这种设计的优势有几个：

第一，**高扩展性**。如果我要新增一个功能，比如"删除好友"，我只需要创建一个新的 `Delete_FriendHandler`，实现 ActionHandler 接口，Spring 会自动发现并注册它，完全不需要修改现有的代码。

第二，**职责分离**。每个 Handler 只负责一个功能，代码清晰，易于维护。

第三，**易于测试**。每个 Handler 可以独立测试，测试覆盖率更高。

### 2.3 BaseActionHandler 抽象基类

**（打开 BaseActionHandler.java）**

为了提高代码复用性，我还设计了 **BaseActionHandler 抽象基类**。

**（指向代码）**

这个基类提供了一些公共方法，比如：

```java
protected void sendResponse(WebSocketSession session, WsResponse response)
protected void sendError(WebSocketSession session, String errorMsg)
protected boolean isAdmin(String username)
```

所有 Handler 都继承这个基类，这样可以避免代码重复，而且保证了错误处理的一致性。

### 2.4 数据存储设计

**（打开 DataCenter.java）**

接下来看看数据存储的设计。

**（指向代码）**

```java
public static final Map<String, WebSocketSession> ONLINE_USERS = new ConcurrentHashMap<>();
public static final Map<String, User> USERS = new ConcurrentHashMap<>();
public static final Map<String, Group> GROUPS = new ConcurrentHashMap<>();
public static final Map<String, Message> MSG_HISTORY = new ConcurrentHashMap<>();
```

我使用了 **ConcurrentHashMap**，这是考虑到 WebSocket 是多线程环境，需要保证线程安全。ConcurrentHashMap 提供了高性能的并发访问，比使用 synchronized 更细粒度的锁，性能更好。

### 2.5 性能优化：线程池

**（打开 AppConfig.java）**

为了提高系统的并发能力，我配置了线程池。

**（指向代码）**

```java
@Bean(name = "chatExecutor")
public Executor chatExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(10);      // 核心线程数
    executor.setMaxPoolSize(20);       // 最大线程数
    executor.setQueueCapacity(200);    // 队列容量
    return executor;
}
```

**（切换到 ChatHandler.java，指向异步处理代码）**

然后在 ChatHandler 中，对于可能耗时的操作，比如发送消息，我使用线程池异步处理：

```java
if ("SEND_PRIVATE".equals(request.getAction()) || "SEND_GROUP".equals(request.getAction())) {
    threadPool.execute(() -> handler.handle(session, request));
}
```

这样可以避免阻塞 WebSocket 的主线程，提高系统的并发处理能力。

---

## 第三部分：核心功能演示（5-6分钟）

### 3.1 用户登录与认证

**（切换到浏览器，打开项目）**

好的，现在让我来演示一下系统的功能。

**（开始操作）**

首先，我打开系统，会看到一个登录界面。

**（输入用户名和密码）**

我输入一个用户名，比如 "alice"，密码是 "123456"。

**（点击登录）**

点击登录。这里有两个情况：

如果是新用户，系统会**自动注册**，不需要单独的注册流程。

如果是已存在的用户，系统会验证密码。密码使用 **BCrypt 加密**存储，这是一种专门为密码设计的哈希算法，即使数据库泄露，也无法还原原始密码。

**（登录成功后）**

登录成功后，系统会显示主界面，左侧是联系人列表，右侧是聊天区域。

### 3.2 私聊功能

**（切换到另一个浏览器窗口，登录另一个用户）**

现在我在另一个浏览器窗口登录另一个用户 "bob"。

**（在 alice 的窗口中，点击 bob 开始聊天）**

回到 alice 的窗口，我可以看到在线用户列表中有 bob。我点击 bob，开始私聊。

**（输入消息）**

我输入一条消息："你好，这是测试消息"。

**（发送消息）**

点击发送。可以看到，消息立即出现在聊天窗口中，而且是**实时**的，不需要刷新页面。

**（切换到 bob 的窗口）**

切换到 bob 的窗口，可以看到消息已经收到了，这就是 WebSocket 实时通信的效果。

### 3.3 消息撤回功能

**（在 alice 窗口中）**

现在演示消息撤回功能。

**（鼠标悬停在刚才发送的消息上）**

我把鼠标悬停在我刚才发送的消息上，会显示一个"撤回"按钮。

**（点击撤回）**

点击撤回。可以看到，消息立即从两个窗口中都消失了，并且显示了一条系统提示："消息已撤回"。

**（讲解时间限制）**

这个功能有一个时间限制，只能在**2分钟内**撤回。让我发送一条新消息，然后等待超过2分钟再尝试撤回，看看会发生什么。

**（发送消息，等待或快进时间）**

**（尝试撤回，显示错误提示）**

可以看到，系统提示"消息已超过2分钟，无法撤回"。这个限制是在后端的 `Recall_MsgHandler` 中实现的，通过比较当前时间和消息时间戳来判断。

### 3.4 群聊功能与 @ 提醒

**（创建群组）**

现在演示群聊功能。

**（点击创建群组）**

我点击"创建群组"按钮，输入群组名称，比如"测试群组"，然后添加几个成员。

**（发送群聊消息）**

创建成功后，我在群组中发送一条消息。

**（演示 @ 提醒）**

现在演示 @ 提醒功能。我在消息中输入 @bob，系统会自动识别这是一个 @ 提醒。

**（发送消息）**

发送后，被 @ 的用户 bob 会在左侧的群组列表中看到红色的提醒标识，表示有未读的 @ 消息。

**（切换到 bob 的窗口）**

切换到 bob 的窗口，可以看到 @ 的文本在消息中会**高亮显示**，非常醒目。

**（演示群主 @ 所有人）**

如果我是群主，我还可以 @ 所有人。输入 @所有人，系统会将消息推送给群组内的所有成员。

### 3.5 敏感词过滤（重点技术）

**（在聊天中输入包含敏感词的文本）**

现在我要演示一个重要的技术点：**敏感词过滤**。

**（输入包含敏感词的文本，比如"你真是个XXX"）**

我在消息中输入一些包含敏感词的文本，比如...（输入敏感词）

**（发送消息）**

点击发送。

**（显示过滤后的结果）**

可以看到，敏感词被自动替换为了星号 `*`，这就是敏感词过滤的效果。

**（讲解 AC 自动机算法）**

这里我使用了 **AC 自动机算法**来实现敏感词过滤。让我简单解释一下为什么选择这个算法。

**（打开 SensitiveWordFilter.java）**

**（指向代码）**

传统的做法是遍历每个敏感词，在文本中查找，时间复杂度是 O(n×m)，n 是文本长度，m 是敏感词数量。

而 AC 自动机算法可以在 **O(n+m)** 时间复杂度内完成，只需要一次遍历文本，就能匹配所有敏感词。

**（讲解原理）**

算法的核心思想是：

第一，构建 **Trie 树**（前缀树），将所有敏感词构建成一棵树。

第二，构建**失败指针**，这类似于 KMP 算法的 next 数组。当匹配失败时，可以通过失败指针跳转到最长公共后缀，避免重复匹配。

第三，匹配过程只需要一次遍历文本，通过失败指针的跳转，高效地完成多模式匹配。

**（指向核心代码）**

```java
TrieNode node = root;
for (char c : text.toCharArray()) {
    // 沿着失败指针跳转
    while (node != root && !node.children.containsKey(c)) {
        node = node.fail;
    }
    // 移动到下一个节点
    if (node.children.containsKey(c)) {
        node = node.children.get(c);
    }
    // 检查是否匹配到敏感词
    if (node.isEnd) {
        // 标记需要替换的位置
    }
}
```

这个算法特别适合敏感词过滤这种多模式匹配的场景。

### 3.6 权限管理

**（登录 admin 账号）**

现在演示权限管理功能。

**（切换到 admin 账号登录）**

我登录系统管理员账号 "admin"，这个账号拥有管理员权限。

**（在群组中）**

进入一个群组，可以看到，作为群主或管理员，我可以对成员进行操作。

**（演示禁言功能）**

我选择一个成员，点击"禁言"按钮，可以设置禁言时长，比如 5 分钟。

**（被禁言的用户尝试发送消息）**

现在这个被禁言的用户尝试发送消息，可以看到，输入框被禁用了，并且显示"您已被禁言，剩余 X 分 X 秒解除"。

**（演示踢人功能）**

管理员还可以踢出成员。我选择另一个成员，点击"踢出"按钮。这个成员会被立即移出群组，并且会在群组中显示一条系统消息："XXX 被管理员移出群组"。

**（讲解权限体系）**

系统实现了三层权限体系：

第一，**系统管理员**（admin 账号），拥有全局权限。

第二，**群主**，拥有群组内的最高权限，可以禁言、踢人、设置管理员、解散群组。

第三，**群管理员**，拥有部分权限，可以禁言和踢人，但不能操作群主和其他管理员。

权限校验是在每个 Handler 中统一进行的，确保安全性。

---

## 第四部分：技术亮点总结（2分钟）

**（切换回 PPT 或总结页面）**

好的，让我总结一下这个项目的技术亮点。

### 4.1 架构设计

第一，**HandlerRegistry 模式**。通过策略模式实现的高扩展性架构，新增功能无需修改现有代码，符合开闭原则。

第二，**BaseActionHandler 抽象**。通过继承实现代码复用，统一错误处理，提高了代码质量。

第三，**线程池异步处理**。对耗时操作使用异步处理，提高了系统的并发能力。

### 4.2 算法实现

第一，**AC 自动机算法**。实现了高效的敏感词过滤，时间复杂度 O(n+m)，适合多模式匹配场景。

第二，**BCrypt 密码加密**。使用专业的密码哈希算法，保证了安全性。

### 4.3 工程实践

第一，**线程安全设计**。使用 ConcurrentHashMap 和 synchronized 保证多线程环境下的数据安全。

第二，**统一的错误处理**。通过 BaseActionHandler 统一错误响应格式，提供了友好的错误提示。

---

## 第五部分：总结与展望（1分钟）

### 5.1 项目总结

这个项目实现了完整的即时通讯功能，包括：

- 用户认证与权限管理
- 私聊、群聊
- 消息撤回、@提醒
- 敏感词过滤
- 完善的权限体系

虽然代码量不大，但重点在于**架构设计**和**代码质量**。通过 HandlerRegistry 模式，实现了高扩展性的架构；通过 AC 自动机算法，展示了算法在实际项目中的应用。

### 5.2 改进方向

当然，这个项目还有很多可以改进的地方：

第一，**数据持久化**。目前是内存存储，重启后数据会丢失。未来可以引入 MySQL 实现数据持久化，使用 Redis 作为缓存层。

第二，**消息队列**。可以引入 RabbitMQ 或 Kafka，实现消息的异步处理和解耦。

第三，**分布式支持**。当前是单机部署，可以通过 Redis 实现多节点部署时的状态共享。

第四，**安全性增强**。可以引入 JWT Token 认证，使用 HTTPS/WSS 加密传输。

第五，**性能优化**。可以实现消息分页加载，支持图片和文件上传，实现离线消息推送。

---

## 【结束语】（30秒）

**（面向评委）**

以上就是我的项目展示。这个项目虽然功能相对简单，但在架构设计和算法应用上做了一些尝试。通过 HandlerRegistry 模式实现了高扩展性的架构，通过 AC 自动机算法实现了高效的敏感词过滤。

谢谢各位老师的聆听，请各位老师批评指正！

**（等待提问）**

---

## 📝 演示注意事项

### 演示前准备

1. **环境检查**
   - [ ] 确保项目能正常运行
   - [ ] 准备好 3-4 个测试账号（alice, bob, admin 等）
   - [ ] 打开 2-3 个浏览器窗口，准备多端演示

2. **代码准备**
   - [ ] IDE 中打开关键文件（HandlerRegistry.java, SensitiveWordFilter.java, ChatHandler.java）
   - [ ] 熟悉代码位置，能快速定位

3. **测试准备**
   - [ ] 提前测试所有功能，确保无 bug
   - [ ] 准备好包含敏感词的测试文本


### 回答问题技巧

1. **认真倾听**
   - 等评委问完问题再回答
   - 如果不清楚，可以请求重复

2. **思考后再答**
   - 不要急于回答，可以稍作思考
   - 如果不知道，诚实承认，但可以说明你的思考

3. **结合代码**
   - 回答问题时可以结合代码说明
   - 展示你的实际理解和实践

4. **展示思考**
   - 不仅要说"是什么"，还要说"为什么"
   - 展示你的设计思考和权衡

---


