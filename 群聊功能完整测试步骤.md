# 群聊功能完整测试步骤

## 🎯 测试目标

完整测试群聊功能，包括：
- 用户注册/登录
- 创建群组
- 加入群组
- 发送群聊消息
- 获取群组信息
- 退出群组

---

## 📋 准备工作

### 1. 启动服务器

```bash
cd /Users/ryllis/IdeaProjects/chat
./mvnw spring-boot:run
```

等待看到 `Started ChatApplication` 表示启动成功。

### 2. 准备测试工具

推荐使用：http://www.jsons.cn/websocket/

**连接地址**: `ws://localhost:8080/chat`

**建议**：打开两个浏览器标签页，分别模拟两个用户（Alice 和 Bob）

---

## 🔐 步骤 1: 用户 A (Alice) 登录/注册

### 1.1 建立 WebSocket 连接

在第一个标签页中：
- 连接地址：`ws://localhost:8080/chat`
- 点击"连接"

### 1.2 发送登录请求

```json
{
  "action": "LOGIN",
  "params": {
    "username": "alice",
    "password": "123456"
  }
}
```

**预期响应**:
```json
{
  "type": "LOGIN_RESP",
  "code": 200,
  "msg": "ok",
  "data": {
    "userId": "alice",
    "username": "alice",
    "role": "USER",
    ...
  }
}
```

✅ **记录**: Alice 已成功登录

---

## 🔐 步骤 2: 用户 B (Bob) 登录/注册

### 2.1 建立第二个 WebSocket 连接

在第二个标签页中：
- 连接地址：`ws://localhost:8080/chat`
- 点击"连接"

### 2.2 发送登录请求

```json
{
  "action": "LOGIN",
  "params": {
    "username": "bob",
    "password": "123456"
  }
}
```

**预期响应**:
```json
{
  "type": "LOGIN_RESP",
  "code": 200,
  "msg": "ok",
  "data": {
    "userId": "bob",
    "username": "bob",
    "role": "USER",
    ...
  }
}
```

✅ **记录**: Bob 已成功登录

---

## 👥 步骤 3: Alice 创建群组

### 3.1 Alice 发送创建群组请求

在 **Alice 的标签页** 发送：

```json
{
  "action": "CREATE_GROUP",
  "params": {
    "groupName": "开发团队",
    "initialMembers": ["bob"]
  }
}
```

**预期响应**:
```json
{
  "type": "GROUP_CREATED",
  "code": 200,
  "msg": "ok",
  "data": {
    "groupId": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
    "groupName": "开发团队",
    "owner": "alice",
    "members": ["alice", "bob"],
    "admins": []
  }
}
```

✅ **重要**: **记录 groupId**，后续步骤需要使用！

例如：`groupId = "a1b2c3d4-e5f6-7890-abcd-ef1234567890"`

---

## 📥 步骤 4: Bob 查看自己的群组列表

### 4.1 Bob 获取群组列表

在 **Bob 的标签页** 发送：

```json
{
  "action": "GET_GROUPS",
  "params": {}
}
```

**预期响应**:
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "groups": [
      {
        "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
        "groupName": "开发团队",
        "owner": "alice",
        "members": ["alice", "bob"]
      }
    ],
    "count": 1
  }
}
```

✅ **验证**: Bob 可以看到自己被加入的群组

---

## 👤 步骤 5: 查看群成员列表

### 5.1 Alice 查看群成员

在 **Alice 的标签页** 发送（使用步骤 3 记录的 groupId）：

```json
{
  "action": "GET_GROUP_MEMBERS",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }
}
```

**预期响应**:
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "groupName": "开发团队",
    "owner": "alice",
    "members": ["alice", "bob"],
    "admins": [],
    "memberCount": 2
  }
}
```

✅ **验证**: 群成员列表正确，包含 alice 和 bob

---

## 💬 步骤 6: Alice 发送群聊消息

### 6.1 Alice 发送消息到群组

在 **Alice 的标签页** 发送（使用步骤 3 记录的 groupId）：

```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "content": "大家好！欢迎加入开发团队！",
    "atUsers": ["bob"]
  }
}
```

**预期响应**（Alice 收到）:
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "msgId": "消息ID",
    "fromUser": "alice",
    "toUser": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "isGroup": true,
    "content": "大家好！欢迎加入开发团队！",
    "timestamp": 1234567890123,
    "atUsers": ["bob"]
  }
}
```

**预期结果**（Bob 应该收到）:
```json
{
  "type": "EVENT_CHAT_MSG",
  "code": 200,
  "msg": "ok",
  "data": {
    "msgId": "消息ID",
    "fromUser": "alice",
    "toUser": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "isGroup": true,
    "content": "大家好！欢迎加入开发团队！",
    "timestamp": 1234567890123,
    "atUsers": ["bob"]
  }
}
```

✅ **验证**: 
- Alice 收到成功响应
- Bob 收到群聊消息通知
- 消息正确标记为群聊（`isGroup: true`）

---

## 💬 步骤 7: Bob 回复群聊消息

### 7.1 Bob 发送回复

在 **Bob 的标签页** 发送（使用步骤 3 记录的 groupId）：

```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "content": "谢谢邀请！很高兴加入！"
  }
}
```

**预期结果**:
- Bob 收到成功响应
- Alice 收到 Bob 的回复消息

✅ **验证**: 群聊消息双向通信正常

---

## 👤 步骤 8: 添加第三个用户到群组

### 8.1 先让第三个用户登录（可选测试）

如果想测试更多功能，可以在第三个标签页：

```json
{
  "action": "LOGIN",
  "params": {
    "username": "charlie",
    "password": "123456"
  }
}
```

### 8.2 Charlie 加入群组

在 **Charlie 的标签页** 发送：

```json
{
  "action": "JOIN_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }
}
```

**预期响应**:
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "groupName": "开发团队",
    "owner": "alice",
    "members": ["alice", "bob", "charlie"]
  }
}
```

✅ **验证**: Charlie 成功加入群组

### 8.3 Alice 发送消息，验证 Charlie 也能收到

在 **Alice 的标签页** 发送：

```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "content": "欢迎 Charlie 加入！"
  }
}
```

**预期结果**:
- Alice, Bob, Charlie 都收到这条消息

✅ **验证**: 群聊消息正确发送给所有在线群成员

---

## 🚪 步骤 9: Bob 退出群组

### 9.1 Bob 退出群组

在 **Bob 的标签页** 发送：

```json
{
  "action": "LEAVE_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }
}
```

**预期响应**:
```json
{
  "type": "SUCCESS",
  "code": 200,
  "msg": "ok",
  "data": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "groupName": "开发团队",
    "message": "已成功退出群组"
  }
}
```

✅ **验证**: Bob 成功退出群组

### 9.2 验证 Bob 不再收到群消息

在 **Alice 的标签页** 发送消息：

```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "content": "这条消息 Bob 不应该收到"
  }
}
```

**预期结果**:
- Alice 和 Charlie 收到消息
- Bob **不应该**收到消息（因为已退出群组）

✅ **验证**: 退出群组后，不再接收群消息

---

## 📜 步骤 10: 测试错误情况

### 10.1 测试非群成员发送消息

让 Bob（已退出）尝试发送群消息：

```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "content": "我应该发送失败"
  }
}
```

**预期响应**（错误）:
```json
{
  "type": "ERROR",
  "code": 500,
  "msg": "您不是该群组的成员"
}
```

✅ **验证**: 非群成员无法发送群消息

### 10.2 测试不存在的群组

```json
{
  "action": "SEND_GROUP",
  "params": {
    "groupId": "不存在的群组ID",
    "content": "测试"
  }
}
```

**预期响应**（错误）:
```json
{
  "type": "ERROR",
  "code": 500,
  "msg": "群组不存在"
}
```

### 10.3 测试群主不能退出群组

Alice（群主）尝试退出：

```json
{
  "action": "LEAVE_GROUP",
  "params": {
    "groupId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }
}
```

**预期响应**（错误）:
```json
{
  "type": "ERROR",
  "code": 500,
  "msg": "群主不能退出群组，请使用解散群组功能"
}
```

✅ **验证**: 群主不能退出，需要先解散群组

---

## 🎉 完整测试流程总结

| 步骤 | 操作 | 用户 | 预期结果 |
|------|------|------|----------|
| 1 | 登录 | Alice | ✅ 登录成功 |
| 2 | 登录 | Bob | ✅ 登录成功 |
| 3 | 创建群组 | Alice | ✅ 群组创建成功，Bob 被加入 |
| 4 | 获取群组列表 | Bob | ✅ 看到新创建的群组 |
| 5 | 查看群成员 | Alice | ✅ 看到 alice 和 bob |
| 6 | 发送群消息 | Alice | ✅ Alice 和 Bob 都收到 |
| 7 | 回复群消息 | Bob | ✅ Alice 收到 Bob 的回复 |
| 8 | 加入群组 | Charlie | ✅ Charlie 成功加入 |
| 9 | 发送群消息（3人） | Alice | ✅ 三人都收到消息 |
| 10 | 退出群组 | Bob | ✅ Bob 成功退出 |
| 11 | 发送群消息（验证） | Alice | ✅ 只有 Alice 和 Charlie 收到 |
| 12 | 错误测试 | Bob | ✅ 非成员无法发送消息 |

---

## 📝 快速测试命令清单

为了方便测试，这里是所有命令的快速参考：

### 用户 A (Alice)
```json
// 1. 登录
{"action": "LOGIN", "params": {"username": "alice", "password": "123456"}}

// 2. 创建群组（记住返回的 groupId）
{"action": "CREATE_GROUP", "params": {"groupName": "开发团队", "initialMembers": ["bob"]}}

// 3. 查看群成员
{"action": "GET_GROUP_MEMBERS", "params": {"groupId": "替换为实际的groupId"}}

// 4. 发送群消息
{"action": "SEND_GROUP", "params": {"groupId": "替换为实际的groupId", "content": "大家好！"}}
```

### 用户 B (Bob)
```json
// 1. 登录
{"action": "LOGIN", "params": {"username": "bob", "password": "123456"}}

// 2. 获取群组列表
{"action": "GET_GROUPS", "params": {}}

// 3. 回复群消息
{"action": "SEND_GROUP", "params": {"groupId": "替换为实际的groupId", "content": "谢谢！"}}

// 4. 退出群组
{"action": "LEAVE_GROUP", "params": {"groupId": "替换为实际的groupId"}}
```

---

## ✅ 测试检查清单

完成所有步骤后，确认：

- [x] 两个用户都能成功登录
- [x] 创建群组成功，初始成员正确加入
- [x] 群成员列表显示正确
- [x] 群聊消息能发送给所有在线群成员
- [x] 群成员可以互相回复消息
- [x] 新成员可以加入群组
- [x] 成员可以退出群组
- [x] 退出后不再接收群消息
- [x] 非群成员无法发送消息
- [x] 群主不能退出群组
- [x] 错误处理正确（群组不存在、用户不在群中等）

---

## 🐛 常见问题排查

### 问题 1: 连接失败
- ✅ 检查服务器是否启动
- ✅ 检查端口 8080 是否被占用
- ✅ 检查连接地址是否正确：`ws://localhost:8080/chat`

### 问题 2: 消息发送失败
- ✅ 确认用户已登录
- ✅ 确认用户是群组成员
- ✅ 检查 groupId 是否正确
- ✅ 查看服务器控制台错误日志

### 问题 3: 消息只发送给部分用户
- ✅ 确认所有用户都已登录
- ✅ 确认所有用户都在群组成员列表中
- ✅ 检查用户是否在 `GET_GROUP_MEMBERS` 的响应中

### 问题 4: 找不到群组
- ✅ 确认 groupId 正确（复制粘贴，不要手动输入）
- ✅ 确认群组已创建（检查 `CREATE_GROUP` 的响应）

---

祝测试顺利！🎉

