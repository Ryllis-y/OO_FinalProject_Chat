# 在线聊天系统 - 用户使用说明书

---

**系统版本**：v1.2 
**编写时间**：2025年12月24日  
**编写团队**：#1f1e33

---

## 目录

1. [运行环境要求](#1-运行环境要求)
2. [系统安装与启动](#2-系统安装与启动)
3. [功能使用指南](#3-功能使用指南)
4. [常见错误与解决方案](#4-常见错误与解决方案)
5. [其他说明](#5-其他说明)

---

## 1. 运行环境要求

### 1.1 服务器端环境

**必需环境**：
- **JDK版本**：Java 21 或更高版本（LTS版本）
- **操作系统**：Windows 10/11、macOS 10.15+、Linux（Ubuntu 18.04+、CentOS 7+）
- **内存**：建议512MB以上可用内存
- **磁盘空间**：至少50MB可用空间
- **网络端口**：8080端口需未被占用

**可选工具**：
- **Maven**：3.8+（可选，项目自带Maven Wrapper）
- **IDE**：IntelliJ IDEA、Eclipse、VS Code（用于开发，运行不必须）

**注意**：
- 本系统不依赖数据库，使用内存存储，服务器重启后数据会丢失
- 系统为单机部署，默认仅支持本地访问

### 1.2 客户端环境

**浏览器要求**：
- **Chrome**：90及以上版本（推荐）
- **Firefox**：88及以上版本
- **Safari**：14及以上版本
- **Edge**：90及以上版本
- **Opera**：76及以上版本

**网络要求**：
- 能够访问服务器IP地址
- 支持WebSocket协议（现代浏览器默认支持）
- 如使用HTTPS，需支持WSS协议

### 1.3 硬件要求

**最低配置**：
- CPU：单核处理器
- 内存：512MB RAM
- 磁盘：50MB可用空间

**推荐配置**：
- CPU：双核处理器
- 内存：1GB RAM
- 磁盘：100MB可用空间

---

## 2. 系统安装与启动

### 2.1 获取项目文件

**方式一：从Git仓库克隆**
```bash
git clone <repository-url>
cd chat
```

**方式二：下载压缩包**
- 解压项目文件到指定目录

### 2.2 检查Java环境

**Windows系统**：
```bash
java -version
```
应显示：`java version "21.x.x"` 或更高版本

**macOS/Linux系统**：
```bash
java -version
```
应显示：`openjdk version "21.x.x"` 或更高版本

**如果未安装Java 21**：
- Windows：从Oracle官网或Adoptium下载JDK 21安装包
- macOS：使用Homebrew安装：`brew install openjdk@21`
- Linux：使用包管理器安装：`sudo apt install openjdk-21-jdk`（Ubuntu）

### 2.3 启动服务器

**方式一：使用Maven Wrapper（推荐）**

```bash
# Windows系统
mvnw.cmd spring-boot:run

# macOS/Linux系统
./mvnw spring-boot:run
```

**方式二：使用Maven命令**

```bash
# 如果已安装Maven
mvn spring-boot:run
```

**方式三：打包后运行**

```bash
# 1. 打包项目
./mvnw clean package

# 2. 运行jar包
java -jar target/chat-0.0.1-SNAPSHOT.jar
```

### 2.4 验证启动成功

**启动成功标志**：
- 控制台显示：`Started ChatApplication in X.XXX seconds`
- 浏览器访问：`http://localhost:8080` 显示登录界面
- 控制台无错误信息

**访问地址**：
- 本地访问：`http://localhost:8080`
- 局域网访问：`http://服务器IP:8080`（需配置防火墙）

### 2.5 停止服务器

- **方式一**：在运行终端按 `Ctrl + C`（Windows/Linux）或 `Cmd + C`（macOS）
- **方式二**：关闭终端窗口（不推荐，可能导致资源未释放）

---

## 3. 功能使用指南

### 3.1 登录与注册

#### 3.1.1 首次登录（自动注册）

**操作步骤**：
1. 打开浏览器，访问 `http://localhost:8080`
2. 在登录界面输入用户名和密码
3. 点击"登录/注册"按钮
4. 系统自动完成注册并登录

**注意事项**：
- 首次登录时，任意密码都会被接受（自动注册）
- 用户名不能为空
- 密码不能为空
- 建议使用易记的密码，因为后续登录需要验证

**界面说明**：
- **用户名输入框**：输入您的用户名（例如：alice）
- **密码输入框**：输入您的密码（例如：123456）
- **登录/注册按钮**：点击完成登录或注册操作

#### 3.1.2 已注册用户登录

**操作步骤**：
1. 输入已注册的用户名
2. 输入对应的密码
3. 点击"登录/注册"按钮

**注意事项**：
- 密码必须与首次注册时的密码一致
- 密码错误会显示错误提示，登录失败
- 支持多用户同时登录（不同账号）

#### 3.1.3 管理员账号

**特殊说明**：
- 用户名为 `admin` 的账号自动获得管理员权限
- 管理员账号首次登录时，任意密码都会被接受
- 管理员在用户列表中会显示特殊标识

**管理员功能**：
- 可以禁言任何用户（全局禁言）
- 可以强制踢出任何在线用户
- 在在线用户列表中显示管理员标识

### 3.2 主界面介绍

登录成功后，进入主聊天界面，界面分为三个主要区域：

#### 3.2.1 左侧边栏

**用户信息区域**（顶部）：
- **用户头像**：显示用户名首字母
- **用户名**：当前登录用户的用户名
- **用户角色**：显示用户角色（管理员会显示"管理员"标识）
- **好友申请按钮**：点击查看待处理的好友申请（有红点提示）
- **退出登录按钮**：点击退出当前账号

**标签页切换**（中间）：
- **联系人**：显示已添加的好友列表
- **群组**：显示您加入的所有群组列表
- **在线用户**：显示当前所有在线用户

**搜索框**（标签页下方）：
- 输入关键字搜索联系人或群组
- 支持实时筛选

**列表区域**（下方）：
- 根据选中的标签页显示对应列表
- 点击列表项可切换聊天对象

#### 3.2.2 中间聊天区域

**聊天头部**（顶部）：
- **聊天对象信息**：显示当前聊天对象的头像和名称
- **状态提示**：显示聊天对象状态（如"对方正在输入..."）
- **操作按钮**：
  - 私聊时显示"删除好友"按钮
  - 群聊时显示"查看成员"按钮

**消息列表**（中间）：
- 显示所有聊天消息
- 自己的消息显示在右侧（蓝色背景）
- 对方的消息显示在左侧（灰色背景）
- 自动滚动到最新消息
- 消息显示时间戳

**输入区域**（底部）：
- **@提醒按钮**：群聊时可用，点击@指定用户
- **消息输入框**：输入要发送的消息内容
- **发送按钮**：点击发送消息
- **快捷操作**：按 `Enter` 键发送消息，按 `Shift + Enter` 换行

#### 3.2.3 右侧信息面板（可选）

部分功能会弹出右侧信息面板，显示：
- 群成员列表
- 好友申请列表
- 其他详细信息

### 3.3 私聊功能

#### 3.3.1 开始私聊

**方式一：从在线用户列表开始**
1. 点击左侧"在线用户"标签页
2. 在在线用户列表中找到目标用户
3. 点击用户名或头像开始聊天

**方式二：从联系人列表开始**
1. 点击左侧"联系人"标签页
2. 在联系人列表中找到好友
3. 点击好友名称或头像开始聊天

#### 3.3.2 发送消息

**操作步骤**：
1. 在消息输入框中输入内容
2. 按 `Enter` 键或点击"发送"按钮
3. 消息立即显示在消息列表中
4. 如果对方在线，会立即收到消息

**消息格式**：
- 支持纯文本消息
- 支持换行（按 `Shift + Enter`）
- 支持中文、英文、数字、标点符号

#### 3.3.3 查看历史消息

**自动加载**：
- 登录后自动加载最近的历史消息
- 切换到聊天对象时自动加载历史消息

**滚动查看**：
- 向上滚动消息列表可查看更早的消息
- 系统会自动加载历史消息（需要时）

#### 3.3.4 消息操作

**撤回消息**：
1. 将鼠标悬停在自己发送的消息上
2. 点击出现的"撤回"按钮
3. 仅2分钟内发送的消息可以撤回
4. 撤回后，消息会在所有端同步删除

**消息反应**：
1. 将鼠标悬停在消息上
2. 点击"反应"按钮
3. 选择反应类型（点赞👍、点踩👎等）
4. 可以取消已添加的反应

### 3.4 群聊功能

#### 3.4.1 创建群组

**操作步骤**：
1. 点击左侧"群组"标签页
2. 点击"+ 创建群组"按钮
3. 在弹出的对话框中输入群组名称
4. （可选）输入初始成员用户名（多个用户名用逗号分隔）
5. 点击"创建"按钮

**注意事项**：
- 创建者自动成为群主
- 群主拥有所有群管理权限
- 群组创建成功后会自动加入群成员列表

#### 3.4.2 加入群组

**操作步骤**：
1. 在"群组"标签页中，点击"+ 加入群组"按钮（如果有）
2. 输入群组ID或群组名称
3. 点击"加入"按钮

**说明**：
- 需要知道群组的ID或名称
- 或等待群主邀请您加入

#### 3.4.3 发送群聊消息

**操作步骤**：
1. 在群组列表中点击目标群组
2. 在消息输入框中输入内容
3. 按 `Enter` 键或点击"发送"按钮

**特点**：
- 群内所有成员都会收到消息
- 消息会显示发送者名称
- 支持@提醒功能

#### 3.4.4 @提醒功能

**操作步骤**：
1. 在群聊输入框中，输入 `@` 符号
2. 输入要@的用户名
3. 或在输入框上方点击"@"按钮，选择要@的用户
4. 发送消息后，被@的用户会收到提醒

**提醒效果**：
- 被@的用户在群组列表会显示红色提醒标识
- 点击进入群聊后，提醒自动清除
- 可以@多个用户

#### 3.4.5 查看群成员

**操作步骤**：
1. 在群聊界面，点击右上角的"查看成员"按钮（👥图标）
2. 右侧会显示群成员列表
3. 列表显示成员名称和角色（群主、管理员）

#### 3.4.6 群管理功能（群主/管理员）

**禁言成员**：
1. 打开群成员列表
2. 点击成员旁边的"管理"按钮
3. 选择"禁言"选项
4. 输入禁言时长（分钟）
5. 被禁言的成员在指定时间内无法在群内发送消息

**踢出成员**：
1. 打开群成员列表
2. 点击成员旁边的"管理"按钮
3. 选择"踢出群聊"选项
4. 被踢出的成员会被移出群组

**设置管理员**（仅群主）：
1. 打开群成员列表
2. 点击成员旁边的"管理"按钮
3. 选择"设为管理员"选项
4. 管理员可以禁言和踢出普通成员

**解散群组**（仅群主）：
1. 打开群成员列表
2. 点击底部的"解散群组"按钮
3. 确认后，群组将被解散，所有成员自动退出

### 3.5 好友管理功能

#### 3.5.1 发送好友申请

**操作步骤**：
1. 切换到"在线用户"标签页
2. 找到目标用户
3. 点击用户旁边的"+"按钮（添加好友图标）
4. 系统自动发送好友申请

**注意事项**：
- 不能给自己发送好友申请
- 已经是好友的用户不会显示添加按钮
- 发送申请后，对方会收到通知

#### 3.5.2 处理好友申请

**查看好友申请**：
1. 点击顶部的好友申请按钮（👥图标）
2. 如有未处理的好友申请，按钮上会显示红点提示
3. 右侧会显示好友申请列表

**接受好友申请**：
1. 在好友申请列表中找到申请
2. 点击"接受"按钮
3. 双方自动成为好友，出现在对方的联系人列表中

**拒绝好友申请**：
1. 在好友申请列表中找到申请
2. 点击"拒绝"按钮
3. 申请被拒绝，双方不会成为好友

#### 3.5.3 删除好友

**操作步骤**：
1. 在联系人列表中点击要删除的好友
2. 在聊天界面顶部，点击"删除好友"按钮（🗑️图标）
3. 确认删除操作
4. 好友关系解除，双方联系人列表同步更新

### 3.6 管理员功能

**前提条件**：需要使用 `admin` 账号登录

#### 3.6.1 全局禁言

**操作步骤**：
1. 切换到"在线用户"标签页
2. 找到要禁言的目标用户
3. 点击用户旁边的"⚙️"按钮（管理按钮）
4. 选择"禁言用户"
5. 输入禁言时长（分钟）
6. 目标用户被禁言，在指定时间内无法发送任何消息

#### 3.6.2 强制踢出用户

**操作步骤**：
1. 切换到"在线用户"标签页
2. 找到要踢出的目标用户
3. 点击用户旁边的"⚙️"按钮
4. 选择"踢出用户"
5. 确认操作
6. 目标用户被强制下线，显示"您已被管理员强制下线"提示

---

## 4. 常见错误与解决方案

### 4.1 启动相关错误

#### 错误1：端口8080已被占用

**错误信息**：
```
WebServerException: Port 8080 is already in use
```

**原因**：
- 8080端口被其他程序占用
- 可能有另一个实例正在运行

**解决方案**：
1. **查找占用端口的进程**：
   ```bash
   # Windows
   netstat -ano | findstr :8080
   
   # macOS/Linux
   lsof -i :8080
   ```

2. **关闭占用进程**：
   - Windows：任务管理器中结束对应进程
   - macOS/Linux：使用 `kill -9 <进程ID>` 命令

3. **修改端口**（可选）：
   - 编辑 `src/main/resources/application.properties`
   - 修改 `server.port=8080` 为其他端口（如 `8081`）

#### 错误2：Java版本不兼容

**错误信息**：
```
UnsupportedClassVersionError: class file version 65.0
```

**原因**：
- 使用的Java版本低于Java 21
- 项目编译时使用了Java 21的特性

**解决方案**：
1. 检查Java版本：`java -version`
2. 安装Java 21：
   - Windows：从Oracle或Adoptium下载JDK 21
   - macOS：`brew install openjdk@21`
   - Linux：`sudo apt install openjdk-21-jdk`
3. 配置JAVA_HOME环境变量
4. 重新启动服务器

#### 错误3：Maven依赖下载失败

**错误信息**：
```
Could not transfer artifact...
```

**原因**：
- 网络连接问题
- Maven仓库访问超时

**解决方案**：
1. 检查网络连接
2. 项目已配置阿里云Maven镜像，应该会自动使用
3. 如仍有问题，手动配置Maven镜像：
   - 编辑 `pom.xml`，确保包含阿里云镜像配置
4. 清理并重新下载：
   ```bash
   ./mvnw clean install -U
   ```

### 4.2 登录相关错误

#### 错误4：密码错误

**错误信息**：
- 界面提示："密码错误"
- 登录失败，返回登录界面

**原因**：
- 输入的密码与首次注册时的密码不一致
- 密码大小写敏感

**解决方案**：
1. 确认使用的是首次登录时设置的密码
2. 检查密码大小写是否正确
3. 如忘记密码，需使用其他用户名重新注册

#### 错误5：无法连接到服务器

**错误信息**：
- 浏览器显示："无法访问此网站"
- 控制台显示："WebSocket connection failed"

**原因**：
- 服务器未启动
- 服务器地址或端口错误
- 防火墙阻止连接

**解决方案**：
1. 确认服务器已成功启动（查看控制台日志）
2. 检查访问地址是否正确：`http://localhost:8080`
3. 检查防火墙设置，确保8080端口未被阻止
4. 如使用局域网访问，确认服务器IP地址正确

### 4.3 功能使用错误

#### 错误6：消息发送失败

**错误信息**：
- 界面提示："连接已断开，请刷新页面"
- 消息无法发送

**原因**：
- WebSocket连接断开
- 网络不稳定
- 服务器重启

**解决方案**：
1. 刷新页面重新连接
2. 检查网络连接
3. 确认服务器正在运行
4. 系统会自动尝试重连，等待几秒

#### 错误7：无法撤回消息

**错误信息**：
- 撤回按钮不可用
- 提示"消息超过2分钟，无法撤回"

**原因**：
- 消息发送时间超过2分钟
- 只能撤回自己发送的消息

**解决方案**：
1. 确认消息是您自己发送的
2. 确认消息发送时间在2分钟内
3. 超过2分钟的消息无法撤回，这是系统设计

#### 错误8：好友申请无响应

**错误信息**：
- 发送好友申请后没有反应
- 对方收不到申请通知

**原因**：
- 对方不在线
- 对方的好友申请列表未刷新
- 已经是好友关系

**解决方案**：
1. 确认对方已登录系统
2. 提醒对方查看好友申请列表（点击👥按钮）
3. 检查是否已经是好友（在联系人列表中查看）

#### 错误9：无法加入群组

**错误信息**：
- 提示"群组不存在"
- 提示"无权限加入"

**原因**：
- 群组ID或名称错误
- 群组已被解散
- 权限限制

**解决方案**：
1. 确认群组ID或名称正确
2. 联系群主确认群组状态
3. 请求群主邀请您加入

### 4.4 权限相关错误

#### 错误10：无权限操作

**错误信息**：
- 提示"无权限操作：只有系统管理员可以禁言用户"
- 提示"无权限操作：只有群主或管理员可以禁言"

**原因**：
- 当前用户权限不足
- 试图执行超出权限范围的操作

**解决方案**：
1. 确认您的用户角色和权限
2. 管理员功能需要 `admin` 账号
3. 群管理功能需要群主或管理员权限
4. 联系具有相应权限的用户进行操作

#### 错误11：无法禁言群主

**错误信息**：
- 提示"不能禁言群主"

**原因**：
- 群主不能被禁言（系统设计）

**解决方案**：
1. 这是系统设计，群主拥有最高权限，不能被禁言
2. 如需限制群主，需要先转让群主权限

### 4.5 界面显示错误

#### 错误12：消息不显示

**错误信息**：
- 发送消息后不显示
- 消息列表为空

**原因**：
- 浏览器缓存问题
- JavaScript错误
- WebSocket连接异常

**解决方案**：
1. 刷新页面（`F5` 或 `Ctrl + R`）
2. 清除浏览器缓存后重新访问
3. 打开浏览器开发者工具（F12）查看错误信息
4. 检查网络连接和服务器状态

#### 错误13：界面样式错乱

**错误信息**：
- 界面布局混乱
- 按钮位置错误

**原因**：
- CSS文件加载失败
- 浏览器兼容性问题

**解决方案**：
1. 刷新页面重新加载资源
2. 清除浏览器缓存
3. 确认使用推荐的浏览器版本
4. 检查浏览器是否禁用了CSS

### 4.6 数据相关错误

#### 错误14：数据丢失

**错误信息**：
- 登录后发现好友列表为空
- 群组列表为空
- 历史消息丢失

**原因**：
- 服务器重启导致内存数据丢失
- 系统使用内存存储，不持久化

**解决方案**：
1. **这是系统设计特点**：系统使用内存存储，服务器重启后数据会丢失
2. 重要数据请自行备份
3. 如需持久化，需升级系统使用数据库存储

---

## 5. 其他说明

### 5.1 系统特性说明

#### 5.1.1 数据存储方式

**内存存储**：
- 系统使用ConcurrentHashMap实现内存存储
- 所有数据（用户、消息、群组）存储在服务器内存中
- 服务器重启后，所有数据会丢失
- 适用于演示和测试，不适合生产环境

**数据持久化建议**：
- 如需数据持久化，建议升级系统使用数据库（MySQL、PostgreSQL等）
- 或定期导出重要数据

#### 5.1.2 多端登录处理

**顶号机制**：
- 同一账号在不同设备登录时，旧连接会被强制下线
- 旧设备会收到"您的账号在另一地点登录，您已被强制下线"提示
- 这是系统设计的安全机制

#### 5.1.3 敏感词过滤

**自动过滤**：
- 系统内置敏感词过滤功能
- 发送消息时自动检测并过滤敏感词
- 敏感词会被替换为 `*` 号
- 过滤过程对用户透明，不影响使用体验

### 5.2 性能与限制说明

#### 5.2.1 系统容量

**当前限制**：
- 建议同时在线用户数：50人以内
- 建议群组大小：100人以内
- 消息存储：受服务器内存限制
- 历史消息：建议定期清理

**性能优化建议**：
- 大群消息广播可能有一定延迟
- 建议定期重启服务器清理内存
- 如遇性能问题，可调整线程池配置

#### 5.2.2 网络要求

**局域网部署**：
- 默认配置仅支持本地访问（localhost）
- 如需局域网访问，需修改前端WebSocket连接地址
- 确保防火墙开放8080端口

**跨网络访问**：
- 需要配置服务器IP绑定
- 需要配置路由器端口转发（如需要）
- 建议使用域名访问（需配置DNS）

### 5.3 安全说明

#### 5.3.1 密码安全

**当前实现**：
- 密码使用简单字符串比较（未加密）
- 密码在传输过程中未加密（HTTP协议）
- 建议仅在信任的网络环境中使用

**安全建议**：
- 不要在公开网络中使用
- 定期更换密码
- 避免使用敏感信息作为密码
- 如需生产环境使用，建议升级为HTTPS/WSS协议

#### 5.3.2 数据安全

**数据保护**：
- 所有数据存储在服务器内存中
- 服务器管理员可以访问所有数据
- 建议在信任的环境中部署

#### 5.3.3 权限安全

**权限体系**：
- 管理员权限：用户名为 `admin` 的账号
- 群主权限：创建群组的用户
- 群管理员权限：群主设置的管理员
- 普通用户：默认权限

**安全建议**：
- 保护管理员账号安全
- 谨慎设置群管理员
- 定期审查权限设置

### 5.4 浏览器兼容性

#### 5.4.1 推荐浏览器

**最佳体验**：
- Chrome 90+（强烈推荐）
- Firefox 88+
- Safari 14+
- Edge 90+

**功能支持**：
- WebSocket协议：所有现代浏览器支持
- ES6+语法：Chrome、Firefox、Safari、Edge完全支持
- Flexbox布局：所有推荐浏览器支持

#### 5.4.2 不推荐浏览器

**不支持的浏览器**：
- Internet Explorer（所有版本）
- 过旧版本的浏览器（Chrome < 90、Firefox < 88等）

**原因**：
- 不支持WebSocket协议或ES6语法
- 可能导致功能异常或界面显示错误

### 5.5 故障排除流程

#### 5.5.1 系统无法启动

**排查步骤**：
1. 检查Java版本：`java -version`（需Java 21+）
2. 检查端口占用：确认8080端口未被占用
3. 检查项目文件：确认所有文件完整
4. 查看错误日志：查看控制台输出的详细错误信息
5. 检查依赖：确认Maven依赖下载完整

#### 5.5.2 功能异常

**排查步骤**：
1. 刷新页面：按 `F5` 刷新浏览器
2. 清除缓存：清除浏览器缓存和Cookie
3. 检查网络：确认网络连接正常
4. 查看控制台：打开浏览器开发者工具（F12）查看错误
5. 检查服务器：确认服务器正在运行
6. 重启服务器：如问题持续，重启服务器

#### 5.5.3 连接问题

**排查步骤**：
1. 检查服务器状态：确认服务器正在运行
2. 检查地址：确认访问地址正确
3. 检查防火墙：确认防火墙未阻止8080端口
4. 检查网络：确认网络连接正常
5. 查看浏览器控制台：检查WebSocket连接错误

### 5.6 技术支持

#### 5.6.1 获取帮助

**文档资源**：
- 本文档：用户使用说明书
- 开发文档：项目开发文档
- API文档：WebSocket API接口文档

**问题反馈**：
- 检查本文档的"常见错误与解决方案"部分
- 查看项目README文件
- 联系开发团队获取支持

#### 5.6.2 日志查看

**服务器日志**：
- 服务器控制台输出运行日志
- 错误信息会显示在控制台
- 建议保存日志文件以便排查问题

**浏览器日志**：
- 按 `F12` 打开开发者工具
- 查看"Console"标签页的错误信息
- 查看"Network"标签页的网络请求

---

## 附录

### A. 快捷键说明

| 操作 | 快捷键 |
|------|--------|
| 发送消息 | `Enter` |
| 换行 | `Shift + Enter` |
| 刷新页面 | `F5` |
| 打开开发者工具 | `F12` |
| 切换标签页 | 鼠标点击标签 |

### B. 系统配置说明

**application.properties配置**：
```properties
# 服务器端口（默认8080）
server.port=8080

# 应用名称
spring.application.name=Simple-Chat-Room

# 消息撤回时间限制（毫秒，默认120000即2分钟）
chat.recall.limit-ms=120000

# 线程池核心线程数（默认10）
chat.thread.core-pool-size=10
```

**修改配置**：
1. 编辑 `src/main/resources/application.properties`
2. 修改对应配置项
3. 重启服务器使配置生效

### C. 常见问题快速参考

| 问题 | 快速解决方案 |
|------|------------|
| 无法启动 | 检查Java版本和端口占用 |
| 无法登录 | 确认密码正确，检查服务器状态 |
| 消息不显示 | 刷新页面，检查网络连接 |
| 无法撤回 | 确认消息在2分钟内，且是自己发送 |
| 好友申请无响应 | 确认对方在线，提醒对方查看 |
| 无权限操作 | 确认用户角色和权限 |

---

**文档版本**：v1.0  
**最后更新**：2025年12月24日  
**编写团队**：#1f1e33

